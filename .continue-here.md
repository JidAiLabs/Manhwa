# Continue here — Per-panel narration SHIPPED + VALIDATED + LOCKED (2026-06-22)

**STATE: per-panel narration is the production default, validated on real Nano Ch1,
user-approved ("looks good, lock it"). Freshness guardrail added + verified on real
data. Both deployed to the Mini (HEAD `a8db1d6`). Suite 801 passed/1 skipped.**

## Live state on the Mini (10.88.0.1)
- HEAD `a8db1d6`. Worker restarted on new code (idle, autopilot=0 → no auto-voice).
  Dashboard restarted on new code (:8170, has the stale-manifest banner).
- Ch1 (id 310) PREPARED with per-panel (status scripted/prepped), QA green, NOT voiced
  (review gate held). render.plan.clean.json is fresh (Jun 22 00:14). Stale Jun-19
  plan moved to `*.STALE-jun19.bak` in the Ch1 dir (delete anytime).
- Verified on real Ch1: densest group 7 panels→7 lines; p000114 "7TH GEN NANO MACHINE"
  system card kept+SHOWN; freshness guardrail: fresh→0 issues, stale→caught.

## NEXT (user decides — none auto-launched)
1. VOICE Ch1 (Qwen) to hear it, OR run the ch1-20 batch → full 317 (per-panel + guardrail
   now active). Enqueue prepare jobs via the dashboard or `jobs.enqueue(con,"prepare",...)`.
2. RESIDUALS (separate follow-ups, NOT the per-panel work): ~19/74 plan segments still
   group 2–5 panels under one line (planner protected-card injection + husk-fallback —
   tighten in timeline_planner if wanted); bad panel crops = upstream YOLO/smart-crop.
3. Optional cleanup: remove the legacy microbeat path (kept switchable, off by default);
   trivial dead-code already removed from manifest_freshness.

## Original implementation chunks (all done, on main)

## What shipped this session (per-panel rolling narration)
Spec: `docs/plans/specs/2026-06-19-per-panel-rolling-narration-design.md`
Plan: `docs/plans/2026-06-19-per-panel-rolling-narration.md`
Decision: narration unit GROUP→PANEL; grouping kept as continuity only; microbeats retired
(switchable flag); system cards = first-class multimodal `panel_kind` (NOT regex).

Commits (all on `main`, 3365cd7..HEAD):
- `66c3965` Chunk 1 — `system` panel_kind in panel_understand (enum+norm+prompt w/ 6 exemplars)
- `e980652` Chunk 2 — `system` NEVER dropped across scene_chrome (the chrome chokepoint),
  story_group keep_by_understanding, render_prep bubble-drop exemption
- `6243340`/`324d0b3`/`599f8de` (+merge `fa40f18`) Chunk 3 — beats emit `panel_narration[]`
  (one {scene_file,line} per panel) + joined `narration`; `align_panel_narration` repair-fill
  guarantees one line/panel; `--understood` wired into beated for grounded pads; loud invariant
  assert `len(panel_narration)==len(scene_files)`
- `be4afc2`/`19c289c` Chunk 4 — punchup per panel-line, preserves `validate_line` grounding
  gate per line + `line_plain`; accepted-panel counter
- `29a0df0`/`9ee1352` Chunk 5 — script_expander one segment+shot per panel (`g{gid:04d}_p{i:02d}`);
  microbeats behind `--microbeats`; error beats still silenced
- `f9bd290`/`704b8ca`/`32e36f6`/`7763f50`/`2bb89c9` Chunk 6 — planner one-cut-per-panel guard;
  prep_qa `system_card_unshown` ERROR (panel_kind-keyed, `_base_scene`-normalized); heal
  invariant guard (re-narration is the gemini subprocess, already aligned); microbeats default-off guard

Final integration review (whole feature): ✅ ready. End-to-end trace intact
beated→punchup→script→planner/TTS; `segment_id` consistent; downstream beat-narration readers
get the kept-in-sync joined string. Two MINOR notes (non-blocking):
- husk-filtered NON-protected panel → narration could play over a neighbor, but QA-gated by
  `montage_flags`; system/story protected panels immune.
- 3-way vs 2-way `min()` for segment count only diverges on the OpenAI path (verbatim prod path
  co-builds paras/tts/shots equal-length, so safe). Note for the future LLM path.

## Chunk 7 — Ch1 validation (HUMAN GATE — do with/for the user)
Per [[confirm-upstream-before-expensive-downstream]] + [[qa-scan-before-rendering]]: prove Ch1
before any batch. Adding `system` is a contract change → understanding MUST be re-run.
1. **Deploy:** `git push` → on Mini `jidailabs@10.88.0.1`: `git fetch && git reset --hard origin/main`.
   (Worker stays paused. Daemon restart NOT needed for tools/pipeline — subprocesses are fresh;
   but worker.py was NOT changed this feature, so no kickstart required.)
2. **Re-run understanding on Ch1:** delete `ongoing/<nano-slug>/Chapter_1/manifest.panels.understood.json`,
   run the `grouped` stage → confirm p000113 ("SKY CORPORATION") and p000114 ("7TH GENERATION
   NANO MACHINE, STARTING ACTIVATION") get `panel_kind=system` in the log.
3. **beated → scripted → planned** on Ch1 (per-panel default, microbeats OFF). Confirm every kept
   panel has its own line; `len(panel_narration)==len(scene_files)` per beat; contiguous `g####_p##`.
4. **Grep the plan cuts** for p000113 + p000114 — both must appear with their text.
5. **Side-by-side** vs the current group/microbeat output (pin the OLD baseline to a microbeats-ON
   prepare for an apples-to-apples clip count): every panel explained, flowing, cinematic, cards
   shown, no repeats, no misalignment. Verify TTS clip count ≈ unchanged.
6. **STOP — user reviews the side-by-side.** Do not batch until approved.
7. **After approval:** reset ch1-20 (`/tmp/start_nano_20.py`), bootstrap worker, 20-ch validation +
   average, then full 317-ch run. Then a cleanup commit removing the legacy microbeat functions +
   the `narration_microbeats` flag.

## Ops / machines (unchanged)
- Mini `jidailabs@10.88.0.1` = M4 Pro/64GB, runs Nano (series 2). gpu=1, OLLAMA_NUM_PARALLEL=4.
  Dashboard token b67667af1ab05755d7e28dcef3a08c2c.
- Air `localhost` = M4/32GB, np=2, different manhwa, paused.
- Fast Ch1 re-plan test (no re-narrate): `python tools/timeline_planner.py --groups … --beats …
  --script … --vision … --out /tmp/x.json --mode narrated --min-cut-sec 3.5` then grep cuts.
- `/tmp/start_nano_20.py` resets ch1-20 when ready to batch.
