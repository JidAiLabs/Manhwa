# Continue Here — Manhwa pipeline (handover)

## ⭐ 2026-06-10 EOD: ALL THREE CHAPTER-1 VIDEOS DONE (pixel-verified, 0/5 bad frames each)
- `ongoing/nano-machine/Chapter_1/render/nano_ch1_FINAL.mp4` — 9.43 min, 108 MB
- `ongoing/omniscient-reader/Episode_1/render/orv_ep1_FINAL.mp4` — 3.39 min, 35 MB (⚠️ ORV episodes are SHORT — bundle 2-3 eps per video for the 10-min target)
- `ongoing/infinite-evolution-from-zero/Chapter_1/render/ie_ch1_FINAL.mp4` — 15.77 min, 158 MB (65 beats; consider a max-length cap/split later)
- Defect stack generalized: ORV prep dropped 1 seam dup / cleaned 16 bubbles; IE dropped 2 / cleaned 141. Elftoon adapter fixed (theme churn: `li[data-num]`, fixture refreshed live, commit b415cd4).
- flash-attn on Modal REVERTED (no prebuilt wheel for our torch/CUDA/py → setup.py 404s; retry = pin torch + install wheel URL from Dao-AILab releases). sdpa stands at ~$0.37/ch; weight-cache volume works.
- **NEXT: dashboard v1** — plan agreed in-session: FastAPI+Jinja+htmx in `studio/dashboard/`, `studio dashboard` CLI; home grid (series×chapters stage bars, DB+artifact-probe merged truth), chapter detail (per-stage costs from stats.usage, QA scorecard, clip stats, artifact links), aggregates ($/chapter, minutes produced); v1 read-only w/ copy-paste commands, v1.5 run-buttons; v2 = uploads table + YouTube Data API metrics for the A/B agent. PENDING user confirms: FastAPI+htmx stack; read-only vs run-buttons in v1.
- **Roadmap after dashboard** (agreed): pipeline stages prepped/rendered into state machine → thumbnails (Remotion renderStill, purple weak→strong MC) → dist/ staging synced to Google Drive + YouTube upload agent (OAuth needed from user; ~6 uploads/day quota) → `studio refresh` + scheduling → AniList trending discovery + completed-series tracking → analytics/A-B agent.

**Last session:** 2026-06-10 · **Focus:** narration quality → a character-aware, dialogue-rich, grounded recap engine. **Status:** narration looks good on Nano ch1; ready to move to TTS + render + productization.

---

## TL;DR — what's true right now (verified)

- **Narration engine rebuilt.** The voiced line is now written by the **image-seeing Gemini pass** (not blind OpenAI), names characters from a **chapter cast**, and **weaves in quoted dialogue**. Nano ch1: 36/36 groups, **0 empty**, 31 name the cast, 29 quote dialogue.
- **A/B settled:** Gemini-only narration (**Variant B**) beat OpenAI-polish (Variant C) judged against the panels — 23/13, fewer invented events. **Decision: Gemini writes the line; OpenAI does NOT reword it.**
- **Closed-loop auto-correction (Stage C) is DISABLED** — it over-flagged and once turned a good line into fantasy ("Elara… chandeliers"). Needs a safety-guard redesign before re-enabling.
- **Tests: 260 passing, 1 skipped.** All changes gated so legacy behavior is unchanged.
- **Review artifacts (open in browser):**
  - `ongoing/nano-machine/Chapter_1/narration_report.html` — final narration vs panels
  - `ongoing/nano-machine/Chapter_1/original_chapter.html` — original source pages
  - `ongoing/nano-machine/Chapter_1/narration_compare.html` — the B-vs-C grounding A/B

## Tools added this session
- `tools/cast_builder.py` — one Gemini call → `manifest.cast.json` (Prince Cheon / the Ancestor / the Assassins / Nano Machine AI), with visual descriptions for matching.
- `tools/narration_report.py` — panels-next-to-voiced-line QA view (the report above).
- `tools/narration_grounding_check.py` — multimodal judge: each line vs panels, flags ungrounded claims; rendered the B/C A/B.
- `tools/narration_refine.py` — Stage C closed loop (judge→regen). **Built but DISABLED** (over-aggressive; see memory `closed-loop-regen-needs-safety-guard`).
- `tools/modal_tts.py` — (prior session) Modal A10G Qwen3-TTS app. **Needs a `modal.Volume` weight cache** (see next actions).

## Tools modified
- `tools/gemini_narrative_pass.py` — added `narration` field (grounded cinematic line); `--cast` (cast block + dialogue-weaving + action-energy + hard grounding "no chandeliers"); `--corrections` (Stage C); **non-empty narration retry guard**; scene_selection rule (c) drops bubble/SFX-dominated panels.
- `tools/script_expander.py` — `--narration-source {legacy,openai_polish}` (openai_polish = Variant C, **not used in prod** since B won); **gpt-5/o-series fix**: chat path now uses `max_completion_tokens` + `reasoning_effort=minimal` (was crashing on `max_tokens`).

## Production narration path (current) — ✅ FULLY IN THE PIPELINE (2026-06-10)
```
studio beated   : cast_builder.py → manifest.cast.json (skipped if exists), then
                  gemini_narrative_pass.py --cast → manifest.beats.json   # unified name, NO refine loop
studio scripted : script_expander.py --narration-source gemini_verbatim --cast → manifest.script.json
                  (DETERMINISTIC, $0, no OpenAI key — narration voiced verbatim, A/B winner B)
```
Both former integration gaps are CLOSED and TDD-tested (suite 260 passed / 1 skipped):
1. ✅ `_stage_beated` builds the cast once (idempotent) and passes `--cast`; beats keep the canonical `manifest.beats.json` name.
2. ✅ `gemini_verbatim` materializer shipped in `script_expander.py`: beats[].narration → sections with aligned `shots`/`tts_paragraphs_v3`/`tts_meta`, deterministic `segment_id` intact (verified against `local_tts_from_manifest.extract_items_from_manifest`). Includes **shout-caps normalization** (OCR "KILL HIM!" → "Kill him!"; cast names keep casing via manifest.cast.json) and **caps/panel-intensity → mood-tag escalation** (neutral tags bump to tense/excited; sad/whisper/etc. never overridden).
- Config knob: `[models].narration_source` in studio.toml — default `gemini_verbatim`; `legacy`/`openai_polish` still available (those still gate on OPENAI_API_KEY).
- **Nano ch1 materialized for real**: `manifest.script.json` regenerated from the confirmed narration (36/36 paragraphs, 0 empty, 0 residual caps, 1,487 words ≈ 11 min, $0.0000). Old artifacts preserved: `manifest.script.legacy-BAK.json`, `manifest.beats.pre-cast-BAK.json` (and `manifest.beats.narr.json` kept; its content now == canonical beats.json).

---

## NEXT ACTIONS (your list, with context)

1. ✅ **DONE — TTS caps normalization.** Shipped inside the `gemini_verbatim` materializer (`normalize_caps_for_tts` + `_escalate_tag_for_intensity` in `tools/script_expander.py`, fully unit-tested in `tests/test_verbatim_script.py`).
2. ✅ **DONE — Voice LOCKED: g0021_p02** (user's pick from their 3-clip shortlist; deepest ~94 Hz, steadiest). Asset: `assets/voice/narrator_ref.wav` + `.txt` transcript. Clone mode wired into `tools/modal_tts.py` (auto-uses the asset; `--voice-ref none` = VoiceDesign exploration) and the qwen backend of `tools/local_tts_from_manifest.py`; `studio.toml [tts].voice_ref` set. Cloning uses `Qwen/Qwen3-TTS-12Hz-1.7B-Base` (VoiceDesign ckpt refuses `generate_voice_clone`; clone mode has NO emotion instruct — delivery = ref prosody + punctuation). ✅ **VOICED (2026-06-10): ch1 = 36/36 clips, 8.9 min, cloned narrator VERIFIED consistent** — F0 median 95 Hz, IQR 92–97 across all clips (ref ~94; old VoiceDesign run sprawled 94–149). Clip conditioning live: uniform 120ms lead-in (max=median=120ms; the old "click → 0.5s nothing" defect is dead), attack median 73% of body; 2 clips auto-lifted, **g0022_p03 hit the +12 dB ceiling and is still soft (~12%) — flagged in tts_index; single-clip re-take is the targeted fix**. Run ≈20 min on A10G ≈ $0.37 (gen-time dominated; log warns flash-attn absent — adding `flash-attn` to the Modal image is the next speed/cost lever). `render.plan.json` rebuilt from the new clips: 36 items, 9.1 min video, 100 cuts, 0 dropped groups → chapter is RENDER-READY (Blender parity render or Remotion port — see grammar/renderer notes). Kokoro `am_puck` (free) remains fallback. See memory `qwen-voice-shortlist`.
3. ✅ **DONE — Modal weight cache.** `tools/modal_tts.py` now mounts `modal.Volume` `manhwa-hf-cache` at `/root/.cache/huggingface` (+ `HF_HOME`); pattern verified against current Modal docs; module imports clean. ⚠️ Live verification pending: first real `modal run` still downloads once (populates the volume); the SECOND run proves the cache (watch for no download + fast cold start).
4. **Blender vs Remotion (renderer).** Current = Blender VSE (`tools/blender_vse_from_plan.py`, consumes `render.plan.json`). Remotion (React/Node programmatic video) is likely better for a *templated* montage: easier transitions/captions/thumbnail templates, data-driven, web-native — at the cost of rewriting the render stage. Brainstorm; it's a real switch.
5. **Shot composition / montage grammar.** Within a narration group: when to cut vs slide vs pop; how many panels on screen at once (single / split / grid) and *why* (beat intensity? dialogue count? panel size?); flashback insertion into earlier groups for intensity + to hit **≥10-min/chapter**. This is the `timeline_planner` + render design — currently emits `cuts[]` + camera/motion but no rich grammar. Brainstorm.
6. **Thumbnail.** Per-manhwa, catchy, reusable across chapters. Your steer: **purple tones**, **weak→powerful MC transition** composition. Likely a thumbnail-generator stage using the protagonist cast image + split "before/after" + bold title. Generate concepts next session.
7. **Generalization / isolation.** Verify the cast-aware narration + new tools work cleanly on EVERY series (Omniscient Reader, Infinite Evolution From Zero), each isolated (per-series cast/config). Only tested on Nano Machine so far.
8. **Control panel.** Web dashboard over the SQLite catalog: per-chapter pipeline progress, QA reports, costs, live visualization. Later: connect to the YouTube channel so an AI agent can compare which outputs perform better (A/B on real metrics).

## OPEN QUESTION answered: acquisition automation
Current surface = `studio` CLI: `add-series <source> <url>`, `list`, `fetch`, `run`, `status`, `qa`. The `--chapters` selector takes `N`, `N-M`, or `new`.
- **Manual** series add by URL — **no auto-search/discovery** of new manhwa.
- Chapters are discovered **once, at `add-series`**.
- `--chapters new` = process discovered-but-unprocessed chapters (NOT "check the site for newly published ones").
- **No** scheduled re-check / poll / cron for newly published chapters on ongoing series.
➡️ **Gap to build:** a `refresh <series>` that re-scrapes the series page and inserts newly published chapters, + optional scheduling and new-series discovery — to make acquisition a hands-off part of the pipeline.

---

## Detector plan state (2026-06-10 afternoon, user-approved Steps 1+2)
- **Baseline (per-class val, 481 imgs): panel P.973/R.937/mAP50-95 .913 · system_box .843 · ⚠️ speech_bubble/sfx/text/character have ZERO val instances** — and zero detections on real pages at any imgsz/conf: those 4 classes were declared in data.yaml but never meaningfully labeled. "Use the classes we paid for" is DEAD for bubbles via our model.
- **Working bubble source found: `ogkalu/comic-speech-bubble-detector-yolov8m`** (pretrained, auto-downloaded by `manhwa-cropper/manhwa_cropper/detectors/bubbles.py`, cached ~/.cache/manhwa_cropper). Verified on Nano ch1: 0.92–0.97 conf on dialogue scenes. Chunk-level needs TILED inference (tall strips squeeze bubbles away; 1024-tall overlapping slices).
- **Shipped (TDD, suite 274 passed):** `snap_panels_to_elements` + `elements_norm` emission in `studio/detect/yolo_panels.py` (dormant until a bubble source feeds it — wire ogkalu tiled), `chunk_box_to_scene_local` mask mapping in `tools/clean_panels_inpaint.py`.
- **Remaining Step 1:** tiled-chunk bubble wrapper (studio/detect) → feed snap at detect stage; per-scene ogkalu masks into clean_panels_inpaint (+ scenes_clean/ render-prep wiring). **Step 2:** carloskgh Roboflow set matters MOST if it has speech-bubble labels (user must check — 403 for agents); retrain yolo26s @960 gated on the val baseline above, after adding bubble/sfx val labels.

## Renders (Nano ch1) — BOTH VERIFIED (pixel-sampled, 0/6 bad frames each)
- **Remotion WORKS end-to-end**: `remotion/` workspace (plan-driven components, parity Ken Burns/blur-bg). `nano_ch1_remotion.mp4` (543.9s, 194MB — set CRF for smaller). Worked on FIRST full run. ~37 min wall @ concurrency 8.
- **Blender baseline fixed after THREE latent bugs** (script had never run end-to-end): (1) Blender 5.0 video API needs `image_settings.media_type='VIDEO'` before FFMPEG; (2) relative strip paths break on .blend reopen → resolve_path now always absolute; (3) `add_blurred_bg(channel_base=channel_safe_start(...))` passed a FRAME as a CHANNEL → black dim stack clamped to ch128 ABOVE the fg → all-black video. All fixed → `nano_ch1.mp4` (548.6s, 114MB).
- Parity note: Blender runs 4.7s longer than the plan (per-cut ceil-rounding + channel collision shifts accumulate); Remotion matches total_duration_sec exactly. **DECISION SIGNAL: grammar work goes to Remotion; Blender = fallback.**

## Render-prep stage (NEW, 2026-06-10 — user's watch-through fixes; suite 283 passed)
`tools/render_prep.py` (plan → scenes_clean/ + render.plan.clean.json), run before the renderer:
1. **Seam-dup filter**: geometric (global coords) + **VISUAL containment** (template match — REQUIRED because `chunk_global_y0` ignores stitch overlap: real p15/p16 pair is "adjacent" in global coords yet NCC 0.9954). Ch1: dropped p000016+p000044+p000075 (user only ever saw the first). Durable upstream fix (scened-stage cross-chunk dedupe) still TODO.
2. **Bubble inpaint**: ogkalu boxes → oval-aware mask (white+black, center-flood, outline dilated) → cv2.inpaint. Ch1: 103 bubbles across 97 shown scenes. ⚠️ v1 quality: big bubbles leave soft TELEA smears (cloud-like) — upgrade path = LaMa/generative fill.
3. **Margin trim + full-bleed**: uniform light borders trimmed; `scene_dims`+`scenes_subdir` emitted in plan; Remotion renders aspect≥1.3 panels cover/edge-to-edge (user preference).
Remotion consumes the clean plan via `--props render.plan.clean.json` (same public-dir). v2 render: `nano_ch1_remotion_v2.mp4` (--crf 22). Side-by-side proof: `render/clean_compare_p000015.jpg`.

## Costs measured (Nano ch1)
cast ~$0.01–0.02 · beats(cast) ~$0.09 (gemini-2.5-flash) · script_C ~$0.014 (gpt-5-nano) · grounding check ~$0.02 (flash-lite) · Modal voicing ~$0.19 (cold-start-dominated; fix with weight cache).

## Memories written this session (auto-loaded next session)
`confirm-upstream-before-expensive-downstream`, `narration-grounding-belongs-in-multimodal-pass`, `closed-loop-regen-needs-safety-guard`.

## How to resume
1. Read this file + the spec `docs/plans/specs/2026-06-10-narration-grounding-design.md`.
2. Open `narration_report.html` to re-see current narration quality.
3. Integration gaps + caps normalization + Modal cache are DONE (2026-06-10, suite 260 green). `manifest.script.json` (verbatim Variant B) is ready to voice. Pick up at NEXT ACTIONS: **confirm voice (#2) → voice it** (Modal-Qwen: `.eval_venv/bin/modal run tools/modal_tts.py --script ongoing/nano-machine/Chapter_1/manifest.script.json --out-dir ongoing/nano-machine/Chapter_1/tts`; or free local kokoro via `studio run`) → then the bigger design threads (Blender-vs-Remotion #4, montage grammar #5, thumbnail #6, generalization #7, control panel #8, acquisition refresh).
4. NOTE: the working tree carries ALL of this uncommitted (plus the Jun-10 narration-engine work) — commit when you're happy.

## Key paths
- Narration tools: `tools/{cast_builder,gemini_narrative_pass,narration_report,narration_grounding_check,narration_refine}.py`
- Nano ch1 data: `ongoing/nano-machine/Chapter_1/manifest.{cast,beats.narr,vision,groups}.json`
- venv: `.eval_venv/bin/python` · creds auto-load from `keys/creds.env` (incl. Modal); Gemini auth via `keys/gcp-vision.json` SA key.
