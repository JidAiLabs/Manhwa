# Continue Here — Manhwa pipeline handover (2026-06-11 morning)

**Session arc:** 2026-06-10→11 marathon: narration v2 productized → all 3 first-chapter videos rendered → user QA found defect classes → fixes built → **user halted the render loop: "QA-first".**

## ⛔ THE DIRECTIVE FOR THIS SESSION (user's explicit instruction)
**Do NOT render anything first.** Instead:
1. **Build the prep-QA scanner** (`tools/prep_qa.py` or extend render_prep): scans `render.plan.clean.json` + `scenes_clean/` per shown cut and flags every known defect class (list below) — art_score, blank_box_frac, chrome leakage, doc/tall/scroll consistency, ghost text. Emit console + JSON/HTML report with thumbnails.
2. **Run the quality check YOURSELF on all 3 chapters, review the report, fix what it finds.**
3. Show the user the QA report. **Only then** re-run the pipeline + render (plan below).
4. Address the YOLO root cause (tiled inference — see below) as part of this round if feasible: it eliminates several defect classes at the source.

## Defect ledger (all USER-reported from watching; status)
| # | Defect | Status |
|---|---|---|
| 1 | Seam duplicates (p15/p16 etc.) | ✅ fixed (geometric + visual containment in prep) |
| 2 | Bubble text visible → then "blurred bubbles" → then ghosts | ✅ fixed (word-box flat-fill inside bubble interiors; bubbles/outlines stay) |
| 3 | White page margins; want full-bleed | ✅ fixed (trim + wide-cover ≥1.3 aspect) |
| 4 | Bubble-only/spiky panels as full-screen scenes | ✅ fixed (dominance gate + midtone/binary part filter) |
| 5 | System message (Sky Corp cliffhanger) dropped/cleaned | ✅ protected (our model's system_box + Gemini bubble_mode arbiter) |
| 6 | Over-merged crops (2 panels + void in one scene) | ✅ split2 / crop-to-art at prep (root cause = detection, see YOLO) |
| 7 | Chrome narrated+shown: "Redice Studio", "VIEWS: 1", giant "1" card, covers | ✅ in code (scene_chrome: counters incl. OCR-confusion 'VIEWS: I', credits, site-plugs 'ELFTOON.COM ED:', markers, title-dominance, garbage-title 'SR OMNISCIENT CE IA ED NEO TR', midtone rule for OCR-blind cards) + narration prompt rule. **NOT yet in any rendered video** |
| 8 | "the web novel reader" → must be "our protagonist" | ✅ cast_builder rule (verified in regenerated cast) |
| 9 | Openings = generic bridge lines | ✅ should vanish once chrome-filtered groups regenerate (opening group = story) — **verify opening narration in QA** |
| 10 | Tall strips (CRASH/ROAR) rendered tiny | ✅ scroll-shot in Cut.tsx (h/w≥2 → full-width vertical travel, tilt_up scrolls up, 15% end-hold) — **never rendered yet** |
| 11 | Document/stats text cover-cropped | ✅ doc flag in scene_dims → never full-bleed/scroll — **never rendered yet** |
| 12 | #22: huge blanked caption boxes dominate frame (feet strip + 2 white voids w/ ghosts) | ⚠️ `dead_box_recrop()` implemented + tested in render_prep.py but **NOT WIRED into main's writer** (wire after cleaning, before split; also use its blank_box_frac in the gate) |
| 13 | Nano empty-bubble closeup frame | ⚠️ Nano final predates ALL gates — needs re-prep+re-render only (voice/narration approved & kept) |
| 14 | ELFTOON.com watermark stamp on source pages | ❌ TODO (template-match inpaint, low priority) |

## 🎯 ROOT CAUSE the user identified: "are we utilizing YOLO properly?" → NO
Detection runs on 800×9536 chunks letterboxed to **640px** → panels are ~50-100px in model space (val mAP .913 was measured on page-sized images — domain mismatch). This causes the over-merges/under-splits/swallowed-captions that prep keeps compensating for. **Fix = tiled inference** in `studio/detect/yolo_panels.py`: slide ~1280-tall windows with ~128px overlap down the chunk, detect per window at imgsz 1280, offset boxes back, merge across seams (IoU-NMS keeping larger) — also kills cross-chunk seam dups at the source. Pure tiling+merge math is TDD-able. NOTE: re-detection invalidates scenes→vision→groups→beats for all 3 chapters (full reprocess ≈ vision $0.5 + beats $0.4 + voice ~$1, hours). Decide with user whether root-fix-now or QA-render-now-then-root-fix.

## Current artifact state (all 3 finals are STALE — predate fixes #7-12)
- `ongoing/nano-machine/Chapter_1/render/nano_ch1_FINAL.mp4` (9.43min) — user-approved narration+voice; visuals predate ALL gates
- `ongoing/omniscient-reader/Episode_1/render/orv_ep1_FINAL.mp4` (2.68min) — chrome-narration mostly fixed; visuals predate #10-12
- `ongoing/infinite-evolution-from-zero/Chapter_1/render/ie_ch1_FINAL.mp4` (14.58min) — same
- Last night's "redo #3" + nano re-render were **KILLED mid-flight** (exit 144) per the QA-first directive. Scripts live at `/tmp/redo_chapters.sh` (Omniscient Reader + Infinite Evolution full chain: groups→cast→beats→script→modal voice→plan→prep→render→studio qa) and `/tmp/nano_rerender.sh` (prep+render only). Logs `/tmp/redo_chapters*.log`, `/tmp/nano_rerender.log`.
- **Omniscient Reader = "Episode 0 (Prologue)"** now in catalog (sqlite `chapter` table, series_id=1, number=0.0). Short (~2.7min) is EXPECTED — it's a prologue; episode-bundling (2-3 eps/video) is the planned fix. Directory stays `Episode_1` (manifests hold absolute paths) — rename only with upload-metadata work.
- **NEVER use acronyms with the user** — write "Omniscient Reader" and "Infinite Evolution", not ORV/IE.

## Key invariants (do not regress)
- Locked narrator = voice-clone of `assets/voice/narrator_ref.wav` (+.txt) via Qwen3-TTS **Base** ckpt on Modal (`tools/modal_tts.py`, sdpa ~$0.37/ch; flash-attn REVERTED — no prebuilt wheel; weight-cache volume `manhwa-hf-cache` works). Branding intro/outro wavs in `assets/branding/origin-power/` auto-inserted by prep.
- script stage = `gemini_verbatim` ($0, no OpenAI); beats = cast-aware Gemini w/ chrome+grounding prompt.
- Renderer = Remotion (`remotion/`, plan-driven; `--props render.plan.clean.json --public-dir <episode>`); Blender = fallback only.
- QA tooling that exists: `studio qa <sid> --chapters N` (montage scorecard), `tools/narration_report.py` (line-vs-panels), `qa_flags`. The chain ran `studio qa` but output was never reviewed — ALWAYS surface it.
- Suite: **323 passed, 1 skipped** (`.eval_venv/bin/python -m pytest -q`). Everything committed through `766daa0` + this handover commit. Working tree should be clean except deliberate untracked (BAK files, eval/, manhwa-cropper/, debug_clean/).

## After QA+rerender: agreed roadmap
Dashboard v1 (FastAPI+Jinja+htmx in `studio/dashboard/`; grid=DB+artifact-probe merged truth; chapter detail w/ stats.usage costs + QA + links; v1 read-only — plan agreed with user, stack confirm pending) → pipeline stages prepped/rendered into state machine → thumbnails (Remotion renderStill, purple weak→strong MC) → dist/ staging + Google Drive + YouTube upload agent (user OAuth needed) → `studio refresh` + scheduling → AniList trending + completed-series tracking → analytics A/B agent. Speed levers queued: batched TTS (generate_voice_clone accepts lists, 2-3×), concurrent beats calls, Remotion Lambda.

## Cost notes
Voice ~$0.37/ch (Modal A10G sdpa) · beats ~$0.09-0.18/ch · cast ~$0.02 · vision OCR ~$0.2/ch · render ~30-50min local CPU. Burned yesterday: ~$3-4 total across redos.
