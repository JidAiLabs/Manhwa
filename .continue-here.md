# Continue here — 2026-08-19 (the narration chain now self-repairs)

## ► STATE: main @ dc11641, Mini pulled + same HEAD. Suite **2162 green**.
Config live: `[detect] yolo_weights = webtoon_panels_v4.pt`, `[render] bubble_shown_mode = "art_only"`,
`art_only_straddle_push_frac = 0.10`. Dashboard http://100.87.229.28:8170
All 4 test chapters at `scripted`. Jobs 166 (ORV Ep0) + 167 (ORV Ep1) verify the last two fixes.

### QA state — FINAL (jobs 161-167 all done, no failures)
| chapter | ERROR | remaining |
|---|---|---|
| Nano ch6 (315) | **0** | — |
| Nano ch1 (310) | **0** | — |
| ORV Ep0 (1) | **0** | — |
| ORV Ep1 (2) | 1 | caption_unvoiced g0022 — a REAL narration miss, not a trap (see below) |

Started the session at 4 / 0 / 2 / 5 ERRORs; ended 0 / 0 / 0 / 1.

## ► THE FINDING OF THIS SESSION — four guards, one failure
The narration chain is heal → writer → punchup → accept_better → plan. **Four** places restore an
incumbent the repair already replaced, and together they made the auto-heal a no-op:
1. `finalize_adaptive_beat`'s pad fallback (`beat = pin_prev`/`prev0`);
2. `narration_accept_better`'s strictly-better judge;
3. the corrections/pin restore in the caller — runs AFTER finalize, so a trim inside finalize never
   sees the version that actually won;
4. `narration_punchup`'s `line_plain` — a shadow of the line BEFORE a punch, restored when a
   candidate is rejected. Any mutator editing `line` and leaving the shadow is silently reverted.

**`[accept-better] judged=11 kept_new=0 reverted=11` is THE signal** — not the individual QA flag.
Diagnostic for #4: the log shows the repair firing (`tightened 59w -> 32w`) on a chapter whose QA
number never moves. `grep -oE "tools/[a-z_]+\.py" <job.log> | uniq` to read the stage order.

## ► Shipped this session (7 commits, 25d921b..dc11641) — each verified on REAL chapter data
1. `2da8fac` a revert guard may never restore a line `usable_narration_line` rejects. Validity
   outranks taste — accept_better does not even consult the judge. (ch1 g0026 stopped voicing
   "The sequence begins with p000110.jpg." after 3 runs of it shipping.)
2. `93c19b4` first actor-gate attempt — **never fired on real data**, superseded by:
3. `f287f17` the actor gate's evidence window = the panel RANGE the segment COVERS, not its span.
   A caption panel is dropped from the shown frames but its words fold in (ORV Ep1 g0021 spans
   p87+p89 and voices `TLS123` printed on p88).
4. `7ab3ca2` the length class repairs itself: shorter-repair-wins + deterministic whole-sentence
   trim (least page-grounded first) + accept_better keeps a measurably shorter rewrite.
5. `e3d2a4c` the trim prefers the best-grounded sentence that actually FITS (ranking alone kept a
   42w sentence and discarded a 31w sibling that fit).
6. `1b9b0e9` the trim runs as a BACKSTOP after the restore decision, on whatever version won.
7. `dc11641` `write_segment_lines` drops a stale `line_plain`; caption coverage outranks the judge.

Measured trim on the 6 real overlong lines (all FIT): 61→28, 59→32, 42→27, 45→31, 73→31, 39→25.

## ► NEXT — one owner decision, then voice
1. **Raise the heal cycle cap (owner call).** The worker's launchd plist pins
   `STUDIO_HEAL_MAX=1`; the code default is 4. The documented reason for pinning it (worker.py:766)
   is *"one heal cycle costs ~35 min and narration_accept_better reverted 12 of its 14 rewrites —
   the loop bought 2 improved lines out of ~85"* — i.e. EXACTLY the bug fixed this session. Post-fix
   accept rates: ORV Ep0 1/1 kept, ORV Ep1 10/19 kept, ch1 2/12 kept. A second cycle is now a
   materially different trade than when the cap was set. Recommend **2**, not 4 (measure first).
   Requires editing the plist + `launchctl bootout`/`bootstrap` — a daemon restart, so it is the
   owner's call; mind the PATH/ffmpeg gotcha ([[worker-ssh-path-ffmpeg]]).
2. **Voice + render ch6, ch1, ORV Ep0** (all green). Voicing needs a STORY approval on the
   dashboard — `gates.voice_allowed` requires an `approval` row and there is none for ANY chapter.
3. Then the remaining WARN classes by count: `grounding_weak` (17 ch6, 16 Ep1, 4 ch1).

### The one remaining ERROR — ORV Ep1 g0022, and why it is NOT the same bug
`caption_unvoiced` on p000095 ("WHAT WAS ABOUT TO HAPPEN TO THIS WORLD.", 14% coverage). The
caption floor works — 9 other groups were kept for covering more (`covers_caption(39->20) kept=new`)
— but g0022's regen did not improve coverage at all, so it went to the judge and drew "equivalent".
This is a writer miss, not a restore trap. Measured caption gap for the beat: p92 6/10 unvoiced,
p94 23/29, p95 7/7, p96 13/14 — most of that is CHAT DIALOGUE the narration correctly summarizes
("As the creator expresses gratitude for his support and offers a special gift"), which is what
RECAP_STYLE_RULES rule 5 asks for. Only p95 is a true narration caption being dropped. Worth
knowing before tuning the gate: `caption_terms` does not distinguish a narration caption from a
speech bubble, so its denominator includes dialogue the writer is SUPPOSED to compress.

## ► STILL OPEN (from the 2026-08-18 seam hunt, none regressed but none fixed)
- `--reuse-clean` hashes derived frames; `scene_dims` aspect changes after aspect-dependent passes;
  `item['scene_files']` is never re-synced to the shipped cuts; positional panel ids;
  `reconcile_seam_panels` deletes scene files.
- v4 ORV recall gap on dark/full-bleed pages — per-series weights vs a v4.1 fine-tune, undecided.

## ► GOTCHAS confirmed again this session
- `_meta.written_at` FIRST before calling anything "stale data" — I called ch1's beats stale and they
  were 3 hours old with the fix live. The bug was a revert guard, not staleness.
- Run a gate against the real manifests before claiming a fix: `pq.actor_mismatch_flags(beats, und,
  cast)` vs `(..., vitems)` side by side caught 93c19b4 doing nothing.
- `tools/` + `studio/pipeline.py` are subprocesses (fresh on pull). `studio/worker.py` +
  `studio/dashboard/**` need `launchctl kickstart -k`. Nothing this session touched those.
