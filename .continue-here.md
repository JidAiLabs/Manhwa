# Continue here — 2026-08-20 (four items closed; seven gate bugs behind them)

## ► STATE: main @ 48d37c4, Mini pulled + worker restarted. Suite **2176 green**.
Dashboard http://100.87.229.28:8170. All four test chapters at **0 ERRORs**;
three are RENDERED from the current plan (`manifest_freshness` says `fresh`).

| chapter | status | ERR | WARN | video |
|---|---|---|---|---|
| Nano ch1 (310) | rendered | **0** | 3 | 80M, plan-fresh |
| Nano ch6 (315) | rendered | **0** | 4 | 62M, plan-fresh |
| ORV Ep0 (1) | rendered | **0** | 2 | 36M, re-rendered (plan sha HAD moved) |
| ORV Ep1 (2) | scripted | **0** | 8 | not voiced — see NEXT |

## ► The four open items from the last handover — all closed
1. **Heal cap** — the launchd pin `STUDIO_HEAL_MAX=1` is REMOVED (plist backed up
   as `.bak-20260820`; worker restarted, PATH intact). Not a tuning choice: BOTH
   convergence guards in `_heal_to_green` first compare on cycle 2, so a cap of 1
   made them unreachable and the heal one blind roll. Uncapped, Ep1/ch6 ran 2-3
   cycles and stopped themselves ("no progress → stopping early").
2. **Voice + render** — ch1 + ch6 voiced (owner-approved) and rendered; Ep0
   re-rendered. Nano is autopilot-OFF by design, so its approvals are explicit.
3. **Ep1 `caption_unvoiced`** — cleared itself once the heal was uncapped. No
   separate fix: the writer was never blind (`ocr_clean` is in its payload) and
   `narration_heal._note_for` already quotes the caption as mandatory.
4. **`grounding_weak`** — 16→1 (Ep1), 17→0 (ch6), 4→0 (ch1), 1→0 (Ep0).

## ► THE FINDING — seven gates were firing on their own blind spots
Every one surfaced as a chapter stuck in production, none as a failing test.
See [[judge-needs-the-writers-evidence]] + [[gates-must-not-block-their-own-remedy]].

- `a6de9ab` the grounding judge is image-only and called on-panel DIALOGUE
  invented (it cannot read painted chat text at frame scale, `art_only` crops
  some out, folded panels are never shown). It now gets the OCR of the COVERED
  panels. **Deleted** `_suppress_grounded_mismatches`' grounding arm — a
  post-hoc guess at that evidence, whose proxy ran BACKWARDS (it demanded the
  narration reproduce ≥50% of the OCR while the rules tell the writer to
  compress it). `semantic_heal = true`, so these phantoms were making the heal
  rewrite CORRECT lines every cycle.
- `40ba09b` **`narration_stale` was a byte-identity tripwire** — difflib's
  autojunk drops every common CHARACTER above 200 elements, so a ~500-char group
  scores 0.10 for a ONE-WORD diff (identical 1.00, one word 0.08, autojunk off
  0.99). The sanitizer's own word swap fired a blocking ERROR re-scripting could
  never clear. `autojunk=False`.
- `40ba09b` **the fold window collapses on a ONE-panel span** (no range to
  widen). Now reaches across panels NO span claims, bounded by `_FOLD_REACH=3`
  (measured: unclaimed runs are 1-3 across all four chapters).
- `40ba09b` every judge parsed model JSON with a greedy `\{.*\}` → "Extra data"
  when the model emits two objects; 5 verdicts lost silently. `first_json`.
- `64a05ed` **`stale_video` promoted to ERROR** deadlocked Ep0: autopilot needs
  zero ERRORs, and only a render clears a stale video. Honour source severity.
- `ab824d2` **`qa_scan` graded with a weaker gate set than `prepare`** — no
  `--semantic`, so `caption_unvoiced` lost its arbiter and a paraphrased caption
  came back a hard ERROR the prepare never reported.
- `48d37c4` **no path to re-render a rebuilt plan**: both advance branches key on
  an INVALID voice approval and the render branch lives in the voiceover handler,
  so unchanged narration + rebuilt plan = stuck forever. New branch + the whole
  block extracted as `_advance_after_prepare`, unit-tested per branch.

## ► NEXT
1. **ORV Ep1 is green and unvoiced.** Its series IS autopilot-on, so a `prepare`
   job will now auto-approve → voice → render it (its voice approval is stale
   because the narration genuinely changed). One job, no decisions needed.
2. Watch whether the uncapped heal costs real wall-clock on a full run — it is
   now bounded by its guards, not by the cap. If a chapter thrashes, the guards
   are the thing to inspect, NOT the cap.
3. Remaining WARNs are cosmetic: `sauce_density`, `pointing_fits`,
   `ocr_echo`, `held_repeat`, 1 `grounding_weak` on Ep1 g0003.

## ► GOTCHAS confirmed again
- `studio/worker.py` + `dashboard/**` need `launchctl kickstart -k`; tools +
  pipeline.py are subprocesses (fresh on pull). Hold the restart while a TTS or
  render job is RUNNING — it kills the child.
- Run a gate against the REAL manifests before claiming a fix. Both of today's
  gate fixes were confirmed that way (`pq.alignment_flags(...)` /
  `pq.actor_mismatch_flags(...)`) before any chapter was re-run.
- zsh does NOT word-split unquoted parameters — `$SSH_CMD` as a bare command
  fails with "no such file or directory".
