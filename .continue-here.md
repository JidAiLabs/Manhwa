# Continue Here — 2026-06-16 (late; marathon session)

## TL;DR
HEAD `3d410be` (Air + Mini synced). Everything committed + deployed. ORV/Nano/IE narration is
cinematic + persona-baseline + tonal-continuity, QA GREEN. Dashboard gained bulk run-range-to-stage
+ duration estimates (INTEGER ep range; lane-aware ~½ wall-clock) + add-by-URL. Voiceover got
inter-group silence. Husk-drop is now deterministic. Logs + job/stage_run/approval history CLEARED
(`studio.db.bak-clearlogs` backup) — user is going chapter-by-chapter. **NOTHING voiced/rendered
yet — gating on the user's review.**

## ★ ACCELERATION — PREMISE WAS WRONG (MEASURED 2026-06-17)
The "~207h / ~40 min-per-chapter render-dominated" premise was an ESTIMATE, never measured —
there were ZERO render_segment rows in history. I measured it on the Mini:
- **Full Nano Ch1 remotion render = 2m28s** (11,792 frames, FPS=30). Render is frame-bound
  (Chromium), encode overlaps and keeps up. **Render is NOT the bottleneck.**
- **GPU encode (videotoolbox) = DROPPED.** It engages in Remotion 4.0.477, but saved ~0–20%
  (noise) AND bloated file size 6× (ignores --crf). Net negative. (Render is Remotion, NOT
  Blender — blender_vse_from_plan.py is legacy/unused by the worker.)
- **Real binding lane = TTS (Qwen voiced ~7min/ch).** prepare-chain ~3.5min, qa_scan ~3.5min
  (gpu lane width 2), render ~2.5min (cpu). Steady-state ≈ TTS-bound → **309 eps ≈ ~36–40h
  (~1.5 days) on ONE machine** — not 413h (broken dashboard ETA) nor 207h.
Full evidence: memory `render-not-bottleneck-tts-is.md`.

**DECISION PENDING (asked user):** ~36–40h single-machine may be acceptable → just run it.
If they want ~½ that, the ONLY real lever is a **2nd physical machine** with its own Qwen-TTS +
Gemma lane (voice is locked to Qwen, one GPU won't parallelize 2 Qwen). Job-claim is ALREADY
race-safe (atomic UPDATE…WHERE state='queued' + rowcount) — the lock was never the issue. The
REAL blocker is **data locality**: ongoing/*/ (2.4GB) is gitignored, Mini-only; Air has code,
not data. A 2nd worker needs network-mounted ongoing/ (DB stays local to Mini, accessed by RPC)
OR per-job rsync. Design only if user opts in.
Then the re-run/voice items below.

## NEXT (re-run to reflect the fixes, then review)
Re-run the test chapters to reflect the accumulated narration/husk fixes, then voice ONE (to hear
the silence + tonal continuity) for the user to approve.

## COMMITS THIS SESSION (newest first)
- `384121e` render: deterministic empty-bubble-husk drop (bubble_cov>=0.20 AND text_cov<=0.02).
  Validated: of 75 shown IE panels drops ONLY p000010 (the husk), no over-drop.
- `679d7f1` voiceover: TONAL CONTINUITY bridge rule (gemini_narrative_pass) + 0.4s silence BETWEEN
  groups in the preview concat (worker.py `_h_voiceover`, format-matched anullsrc).
- `7df46a7` style: dark input/select for the new dashboard widgets.
- `89a40a0` dashboard: bulk "run range to stage" (target qa|voice|video → `auto_to` on each prepare;
  worker `_h_prepare`/`_h_voiceover` honor auto_to to advance past gates up to target) +
  GET `/partials/duration-estimate` (proc time + final video length) + POST `/add-series-direct`.
- `00ddf44` narration: never end on a dangling quoted stub (Max-Level fragment_dangle).
- `62d2dff` narration: multi-panel action beats = cinematic SET-PIECES (fuller, not compressed).
- `3ca97be` narration: cinematic is the BASELINE, persona an OCCASIONAL seasoning (user's stated idea).
- `1ec161c` perf: cache render_prep's Gemma visual judge across heal cycles (`--reuse-clean`).
  Heal cycle ~8min→~1min; Nano 44min→12min. THE fix for the recurring "2.5h" slowness.
- `394a9b3`/`f12172d` heal: QA-eyes→auto-heal + strictly-better safeguard — BUILT but PARKED
  (off by default, `[heal].semantic=false`). Validated correct + safe, but context-blind + ~2.5h;
  the comparison report concluded NOT worth it. Leave dormant unless user wants hands-off-at-scale.
- earlier: `cd64fe9` clipped-edge bubble blank; `0f198f3`+`9951718`+`b560e46` in-world-screen
  rescue + keep-text (ORV masterpiece p000003 shows + narrated); `445519c` checkpoint.

## CHAPTER STATE (Mini studio.db)
- ch1 ORV Ep0 — scripted, cinematic+persona, GREEN. (composite UNLOCKED for the cinematic regen;
  `manifest.beats.composite-approved-BAK.json` + `precinematic-BAK` saved.) NOT yet re-run with
  tonal-continuity/husk/silence.
- ch310 Nano Ch1 — scripted, cinematic, GREEN (job 36, 11.9 min via the cache).
- ch625 IE Ch1 — scripted, cinematic, GREEN — BUT still SHOWS husk p000010 (the husk fix needs a
  render_prep re-run to drop it).
- ch724 Max-Level Ep1 — scripted, FAILED (job 37): fragment_dangle (now fixed in code) +
  panel_substituted (p000000 OVER-dropped — separate OVER-drop bug, NOT fixed).
- series 5 "Tutorial Tower of the Advanced Player" — 177 chapters discovered this session, untouched.

## NEXT
1. Re-run the test chapters to apply the accumulated fixes:
   - tonal-continuity + cinematic refinements → re-narrate (reset to 'grouped', enqueue prepare).
   - husk-drop (IE p000010) → at least a render_prep re-run (reset to 'scripted', rm render.plan*/
     prep_qa, enqueue prepare).
   - Max-Level → re-run; the fragment fix should clear fragment_dangle. If panel_substituted persists,
     investigate WHY p000000 is dropped (over-aggressive husk/judge — the OVER-drop, still open).
2. Voice ONE chapter (Qwen) so the user HEARS the inter-group silence + smoother transitions → review
   → approve. NOTHING is voiced/rendered yet; the user gates voicing.
3. Real 300-ep runs: use the new dashboard bulk control on /series/{sid} (ep range + target + run range).

## OPEN / DEFERRED
- **Max-Level panel_substituted (p000000 OVER-drop)** — husk/judge over-fires on a real panel; the
  under-drop (p000010) is fixed, the over-drop is NOT.
- Cinematic expansion is per-beat LLM judgment (temp 0.7) → uneven (some action beats expand, some
  don't); user: narration "looks okayish, will decide after listening".
- Dashboard ETA widget is BROKEN (shows ~2:5x for everything) — ignore it; real times are on DONE rows.
  (A real follow-up: fix or hide that estimate so it stops false-alarming.)
- Dashboard minimalist redesign: did form-control styling; further restyle is subjective — get direction.

## MECHANICS
- Mini `jidailabs@10.88.0.1` (~/repos/Manhwa, studio.db). Air = control. Dashboard http://10.88.0.1:8170
  (token `STUDIO_DASH_TOKEN`). worker.py / dashboard/** change → kickstart BOTH daemons
  (`launchctl kickstart -k gui/$(id -u)/com.originpower.{worker,dashboard}`). tools/+pipeline.py fresh
  on pull. Deploy = commit→push→`git pull` on Mini (NEVER rsync). Always keep both synced.
- Re-narrate: reset chapter to 'grouped' (keeps understanding), enqueue prepare. Re-prepare only:
  reset to 'scripted', rm render.plan*+prep_qa, enqueue prepare. **BACK UP `manifest.beats.json`
  before ANY narration op** (ongoing/ gitignored → otherwise unrecoverable). Lock approved narration
  with `.narration_keepbase`.
- Detached Mini monitor pattern for long jobs: `nohup .eval_venv/bin/python /tmp/mon.py > log &` then
  poll the log (ssh drops kill a foreground monitor but not the worker jobs).
