# Continue here — 2026-08-18 (v4 + art_only LIVE; QA-gate audit shipped; 4 chapters re-run)

## ► STATE: main @ dfd50b2, Mini pulled + same HEAD. All 4 test chapters at `scripted`, prepare DONE.
Config live: `[detect] yolo_weights = webtoon_panels_v4.pt`, `[render] bubble_shown_mode = "art_only"`,
`art_only_straddle_push_frac = 0.10`. Suite 2140 green. Dashboard http://100.87.229.28:8170

### Re-run results (jobs 157-160, all DONE, no failures)
| chapter | job | ERROR | remaining codes |
|---|---|---|---|
| Nano ch6 (315) | 158 | **0** | — fully green, ready to voice |
| Nano ch1 (310) | 157 | 4 | line_overlong g0023 + filename_in_narration/shot_description x3 (STALE g0026 lines, see below) |
| ORV Ep0 (1) | 159 | 2 | line_overlong x2 |
| ORV Ep1 (2) | 160 | 5 | line_overlong x3, actor_mismatch p000087 ('tls' vs protagonist), caption_unvoiced p000095 (real) |
ch1's twin re-frame fired twice (`re-framed IN PLACE`) — the duplicate-frame report is fixed.

### Shipped this session (13 commits, e9b38d6..dfd50b2) — each verified on REAL chapter data
1. `3e16143` actor gates: grammatical role, not word position (ch1 1->0, ch6 5->0 false ERRORs)
2. `2045d95` narration_offset: impact boolean corroborates, never decides (ch1 1->0, ch6 2->0)
3. `34c24ca` caption denoise + impact scoped to the strike RUN (ORV Ep0 impact 1->0, Ep1 captions 2->1)
4. `75218d4` twin re-frame IN PLACE (my job-156 regression: a new shown filename = 4 blocking errors)
5. `b0ec544` auto-heal wedge (BLOCKER): worker protection now mirrors render_prep's narrated set
6. `dfd50b2` visual verdict caches keyed on PIXELS (frame_digest), not filename
7. `defd206` solo system card exempt from the per-panel voice floor
8. `723ef16` cast names must be ON the page (OCR geometry + sanitizer) — killed the invented "Alden"
9. `77255bc` identity oracle: colour classes + hair evidence (prince != master); cards -> panel_kind=system
10. `26fc10d` tinted panels carry no colour evidence; system cards SPEAK their text; a line must END
Audit evidence: workflow wf_ab1fae2f-401 (5 agents) + scratchpad qa4/ + dup_scan.json.

## ► OPEN DECISION (owner) — `line_overlong`, the only REAL error class left
6 cases (ch1 g0023 61w; ORV Ep0 59w/42w; Ep1 45w/73w/39w) — 5 of 6 are 1-panel 1-SEGMENT beats, so
widening the span or spilling a sentence has nothing to work with, and the panels are WIDE so the
renderer ignores motion (no ken-variety rescue). Cause: bubble-only caption panels fold their WORDS
into a neighbour without their panels. Options: (1) deterministic TIGHTEN, drop the least
page-grounded sentence, guard = never drop a sentence carrying on-page words [RECOMMENDED];
(2) SHOW the folded caption cards (contradicts art-only); (3) accept (non-blocking).
See [[line-overlong-is-upstream]].

## ► ALSO OPEN
- **ch1 g0026 stale lines**: beats still hold "The sequence begins with p000110.jpg." from an older
  fallback. The fallback SCRUB is fixed (57e9ae8) but the stored lines need a re-narration:
  `python -m studio reset 2 --chapters 1 --to grouped --enqueue` (NOT `--to detected`, see
  [[reset-to-detected-trap]]).
- ORV Ep1 actor_mismatch p000087 ('tls' named as actor; TLS123 is a cast member) — verify before fixing.
- ORV Ep1 caption_unvoiced p000095 — the audit called this one REAL (a dropped caption).
- Seam-hunt majors not yet fixed: `--reuse-clean` hashes a derived frame; scene_dims aspect changes
  AFTER the passes that used the old aspect; `item['scene_files']` never re-synced to the shipped cuts;
  panel ids are a positional counter (re-detect renumbers); reconcile_seam_panels DELETES scene files.
- v4 ORV recall gap (dark/full-bleed pages) — per-series weights vs v4.1 fine-tune, still undecided.

## ► NEXT
1. Owner picks the line_overlong option; implement + re-prep the 3 affected chapters.
2. ch1: re-narrate g0026 (reset --to grouped), re-QA.
3. Then voice + render ch6 first (it is already ERROR-free), review, and widen.

## (pre-deploy state below)

## ► CURRENT STATE — commit `e9b38d6` on main (Air; NOT pushed, NOT deployed; Mini healthy+idle on 22c0275)
**Sign-off report:** https://claude.ai/code/artifact/68944a54-1f9e-4d8c-b435-d346fc36abaa
Full log: `docs/plans/2026-07-23-art-only-detector-retrain.md` §2026-08-17. Memory: [[art-only-detector-retrain]].

- v4 = `assets/models/webtoon_panels_v4.pt` (committed; studio.toml still v3). Fair split
  (`dataset_v4/valid_clean`, 616 imgs v3 never trained on): panel AP50 .301→.947, all
  .777→.880, system_ui a wash. Naive split was contaminated (57 imgs incl. 30/50 system_ui).
- **Nano Machine: clear win** (0 missed panels / 30 chunks, spurious 22→12, art-only).
  **ORV: RECALL REGRESSION** — 25 real panels unboxed on 17 chunks (dark/full-bleed/UI
  pages; conf/tiling knobs don't fix it); Ep2 chain: orphaned OCR words 84→116, 36
  recovered strips (no art box → keep display). Training-domain gap → v4.1 needs
  ~150–300 art-only-annotated dark/full-bleed pages (Roboflow; NEVER pipeline output).
- Seam fix shipped in e9b38d6 (yolo_panels `_tile_elements` + `snap(attach_gap)` +
  `panels_norm_art`): elements_norm was EMPTY on real chunks for BOTH models → snap was
  a no-op fleet-wide; ch1: v4 orphaned words 44→33 (v3 37); v3 output byte-identical.
- Phase 4 shipped in e9b38d6: `render_prep --bubble-shown-mode art_only` (+prep_qa,
  `[render] bubble_shown_mode` config → worker flag, one weights authority
  `yolo_panels.default_weights()`). Suite 2091 green. Demo: 46/114 ch1 frames change.
- Tools: `scratchpad/seam/{run_seam.py,run_seam2.py,demo_art_only.py}` (SEAM_CORPUS/
  SEAM_OUT env), `d8_compare_v3v4.py` now writes summary.json; overlays+val JSONs in
  `webtoon-ai/dataset_v2/audit_v4/compare_final/`.

## OWNER DECISIONS (blocking; do NOT swap/deploy before): (1) sign off v4 (2) art_only
globally vs trial chapters (3) straddling bubbles: keep whole (current) vs allow ≤N% art
trim (4) ORV recall: ship globally / per-series weights / hold for v4.1.
**Deploy recipe once signed off:** `git push` → Mini `git pull` → studio.toml
`[detect] yolo_weights = "assets/models/webtoon_panels_v4.pt"` (+ optionally `[render]
bubble_shown_mode = "art_only"`) → `launchctl kickstart -k gui/$(id -u)/com.originpower.worker`
(worker.py changed) → `reset --to detected` the trial chapters (art boxes need a re-detect)
→ prepare → review shown frames on the dashboard → voice.

## Session notes
- Air Tailscale was STOPPED at session start (`open -a Tailscale`; a ping reply to
  100.87.229.28 without it is CGNAT noise). Mini: 473GB free, launchd worker/dashboard/
  ollama fine, gpu_doctor lists itself as ORPHAN (cosmetic bug, ignore/fix later).
- Workflow agents hit the account session limit once (resets hourly-ish); resume with
  `resumeFromRunId` replays finished agents from cache.
- Tech debt noted (not fixed): keep/art_only `_cleaned` recrops don't offset word_boxes
  passed to `_write_part` (focal dead-regions slightly off; pre-existing in keep).

---

# (previous handover 2026-07-29 below)

## ► CURRENT STATE: v4 art-only training running on the AIR
**This session (2026-07-29, Mini OFF — all Air-local):** Phase 1 audit + clean merge
DONE, Phase 2 training LAUNCHED. Full results + corrected premise appended to
`docs/plans/2026-07-23-art-only-detector-retrain.md` (§2026-07-29).

- **Training run:** `/Users/anka/webtoon-ai/runs/detect/webtoon/v4_artonly_960/`
  (pid in `v4_artonly_960.pid`, log `.trainlog`, per-epoch `results.csv`). v3 recipe
  exact: yolo26n init, MuSGD, batch 4, imgsz 960, patience 20, seed 0, mps.
  If dead and incomplete: resume with the same yolo cmd + `resume=<run>/weights/last.pt`.
- **dataset_v4 built + gated** (`webtoon-ai/dataset_v4`, report.json): 3564/673 imgs,
  panel 2940/528 boxes **100% art-only trio** (Manhwa-Base 1336 + manhwa.v1-dataset
  1152 + Webtoon-Manhwa-Panels 980-after-dedupe). Tools:
  `dataset_v2/build/d8_{audit_overlays,assemble_v4,compare_v3v4}.py`; overlays in
  `dataset_v2/audit_v4/`.
- **KEY CORRECTION:** sysbox class-0 was NEVER panels (41px-tall text lines; 87%
  under 0.10 img height) — the "owner gold 120 art-only panels" premise was wrong;
  v3 also swallowed 1161 text-line boxes as panel GT. v4 drops ssys cls-0, keeps its
  bubble/system/sfx. Detect.v4 EXCLUDED (grayscale+90°-rotation augment poison).
  Webtoon-Manhwa Panels KEPT (plan guessed wrong — it IS art-only). manhwa.v1i +
  Manhwa Panel Detector EXCLUDED (9/11 boxes, ~300 falsely-negative images in v3).
- **Phase 3 NEXT:** `d8_compare_v3v4.py --b <run>/weights/best.pt` on
  `dataset_v2/corpus/nano-machine/Chapter_1/scenes` (owner-approved ch; scenes judge
  the art-only convention) + chunks (recall). Prod settings mirrored (conf .25, ckpt
  imgsz). Owner sign-off gates any weights swap in `studio.toml [detect]`.
- **Phase 4 (needs Mini):** `--bubble-shown-mode art_only` display crop in
  render_prep — protect `caption_box`/`system_ui`, over-art bubbles fall back `keep`,
  NEVER materialize art-only scenes (OCR runs upstream on full scenes — unaffected).
- **OWNER DIRECTIVE 2026-08-04: Mini unavailable "for a while" → after training
  completes, do the Air-local validation (final metrics + v3-vs-v4 corpus comparison
  + owner review) and then STOP. Phases 4–5 + any weights swap wait for Mini access.**

---

## SHIPPED this session (2026-07-23, all on main, deployed to Mini)
- **`22c0275` impact_mismatch → gemma's strike read.** Gate now triggers on
  `strikes_or_weapons=='in_use'` (gemma's own read), NOT the CV `impact_lettering`
  detector (fired on any big red glyph, couldn't tell throne-boom from stab). **ch6
  impact_mismatch 25→0, zero re-understanding, verified LIVE** (qa_scan job 126). Memory
  [[impact-mismatch-uses-model-strike-read]]. CV detector now **vestigial** → Phase-2
  retire (needs PROMPT_VERSION bump + re-understand). Validated gemma reads Korean SFX
  the CV+OCR miss + flags a real stab `physical_hit:true`.
- **`fefb96c` actor_mismatch multi-token-name dedup.** "Cheon Yoo Jong"→cheon/yoo/jong
  no longer triple-counts; `subject_actor_nouns` dedups by resolved member. ch6
  actor_mismatch 3→1. Fixes actor_mismatch + dead_actor.
- **`1d7af27` drop-guard root fix (ch6 long_hold).** A drop from ANY source can't orphan
  a narrated line — render_prep re-runs `protect_narrated_from_junk` AFTER the drop
  files; `_heal_visual_drops` never drops a narrated sole-cut + writes `auto_drops.json`
  (≠ operator `manual_drops.json`). **ch6 re-run: long_hold GONE, p17 shown, no manual
  un-drop** (verified job 125). Memory [[held-image-narrated-protection]].
- **ORV Ep1 (id 2) job 123 = done on ollama, OOM=0** across the whole run (grouping fix
  874a712 cleared the phantom-beat wall; ollama switch f9ff65f fixed the OOM). Parked at
  `scripted` with content-QA to review (impact_mismatch will now be ~0 on re-scan).

## ch6 (nano id 315) current state — QA-refreshed, near-green
At `scripted`. After job 126 qa_scan the ERROR flags are **7 (was 34), ZERO real
narration errors**: `actor_mismatch:1` (Cheon oracle-miss FP — panel names him on a
card, narration correct, understanding used a generic handle), `actor_count_mismatch:1`
(plural about off-panel enemies, FP), `narration_offset:2` (minor 1-panel), 
`narration_stale:3` (epithet "wretch"↔"bastard"; the heal edited beats, script.json not
regenerated — needs the scripted-stage regen). Ready for owner review/voice, OR fix
narration_stale properly (see open items).

## OPEN follow-ups (agnostic fixes surfaced, owner to prioritize)
- **Oracle reads intro-cards** (the actor_mismatch root, agnostic): panel understanding
  should resolve a figure to a cast member when the panel's nameplate OCR names them
  ("CHEON YOO JONG", "SEOB MENG"). Kills the actor_mismatch FP class for every named
  character. "Fix the oracle, not the gate."
- **narration_stale agnostic fix:** the heal loop edits beats without re-running the
  `scripted` stage, so the plan can voice stale narration. Fix = regenerate script.json
  when beats change (heal → re-run scripted). Currently trivial (epithet only) but the
  guardrail is real.
- **Phase-2 CV impact detector retirement** (see [[impact-mismatch-uses-model-strike-read]]).
- **line_overlong splitter** (global, deterministic — heal can't beat writer density).
- **ORV review** (impact_mismatch will now be ~0; re-scan + owner review).
- **Security (owner's call):** rotate creds (GCP SA + `keys/creds.env`); Remotion
  4.0.496; CSRF Origin; SSRF allowlist. torch/transformers HELD.

## INFRA / deploy rules
- **Mini access:** `ssh -o IdentitiesOnly=yes -i ~/.ssh/id_ed25519_git-only jidailabs@100.87.229.28`
  (repo `~/repos/Manhwa`). Dashboard **http://100.87.229.28:8170** (Tailscale). [[mini-remote-access-network-saga]]
- **Deploy:** `tools/` + `studio/pipeline.py` are subprocesses = **fresh on git pull**
  (prep_qa, cast_identity, render_prep, panel_understand all subprocess → no restart).
  **`studio/worker.py` + `studio/dashboard/**` + `studio/catalog/reset.py` need
  `launchctl kickstart -k gui/$(id -u)/com.originpower.worker`** (env changes need
  bootout→wait-until-gone→bootstrap, race gives "Input/output error").
- **Backend:** worker plist `OLLAMA_HOST=:11434` (real ollama gemma4:26b, evicts idle);
  MLX shim retired to `originpower-disabled/`. `STUDIO_GPU_WIDTH=1`, `STUDIO_HEAL_MAX=1`.
  gemma vision needs `think=False`. `python tools/gpu_doctor.py` = proc/mem/verdict.
- **Air = dev/control seat only** (decommissioned as factory). E2E/live tests on Mini
  ONLY; unit pytest fine on Air (`.eval_venv/bin/python -m pytest -q`, **2077 pass**).
- Mini currently on `22c0275`. Nothing running (ORV done, ch6 qa_scan done).
