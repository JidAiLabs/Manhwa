# Continue Here — OriginPower Studio (2026-06-12 night, MID-MIGRATION to claw-mini)

## State: factory complete + $0/local; migrating to Mac mini M4 Pro 64GB ("claw-mini")
- Repo rsynced to mini ✓ (4.1G at jidailabs@172.30.1.42:repos/Manhwa — size-verified complete).
- WireGuard Air↔mini UP (Air utun7/wg1.conf 10.88.0.2 ↔ mini wg0 10.88.0.1; fresh keypairs both sides; handshake confirmed).
- THIS machine (the MacBook Air, 32GB) still runs dashboard+worker (pids vary; `pkill -f "studio dashboard"; pkill -f "studio worker"` once mini is live).

## NEXT STEPS (in order, mostly ON THE MINI)
1. Top-up rsync from Air (idempotent): `rsync -a ~/repos/Manhwa/{keys,ongoing,studio.db} jidailabs@172.30.1.42:repos/Manhwa/` + add `YOUTUBE_API_KEY=...` to keys/creds.env (lives only in Air's shell profile!).
2. On mini: `cd ~/repos/Manhwa && scripts/bootstrap_mac.sh` (brew, ollama+gemma4:26b 17G pull, 3 venvs from lockfiles, npm, suite must be 445 green).
3. `scripts/launchd/install.sh <token>`; edit dashboard plist `--host 0.0.0.0` → `--host 10.88.0.1`; reload. Air: http://10.88.0.1:8170/login (POST form).
4. Per series setup: thumbnails exist for 3 series; enqueue `branding_segments` job per series (renders intro/outro mp4 once → singles auto-concat).
5. Press "prepare ALL chapters" on Omniscient Reader → ladder strategy (memory: publishing-strategy-ladder): backlog ≈ 8 days on M4 Pro (likely less), Season-1 pack debut, (0–N) ladder, singles at edge, FULL at end.
6. Queued builds: Apple-OCR backend in vision_extract (validated 87.6% recall, kills last per-chapter API cost); gpu lane split llm/tts (LANES map, ~30% faster on 64GB); YOLO tiled inference; YouTube OAuth upload (v1.1); competitor-YT-scan column.

## Decisions FINAL this session (do not relitigate)
- Gemma 4 26B local = default writer everywhere (beats/cast/meta/punchup; studio.toml beats_backend=ollama). NEVER pull -mlx tags (text-only).
- Qwen locked clone = narrator, local MPS (Modal retired). Kokoro = config option only.
- Publishing = render-once/concat-everything ladder (memory file). 1 thumbnail per manhwa, no licensed names anywhere.
- Punch-up persona pass built, genre-aware (murim/modern/system axes) — humor level + voice-test verdict still PENDING user.
- Two-approval operator flow: story (pre-voice, estimate-plan QA report) → voiceover (listen) → auto-render. Worker enforces all gates + 3 lanes (gpu/cpu/api).

## Key facts
- Mini: jidailabs@172.30.1.42 (KT router, DHCP — PIN THE IP), tunnel 10.88.0.1, hostname CLAW-MINI. Air = anka, 10.88.0.2.
- Dashboard auth: STUDIO_DASH_TOKEN env + POST /login form (compare_digest, samesite=strict).
- Suite: `.eval_venv/bin/python -m pytest -q` → 445 passed. Committed through 17b145a. Untracked-by-design: eval/, manhwa-cropper/, logs/.
- Costs: $0/chapter all-local; only thumbnails ($0.13/series, owned) + AniList/YouTube read-only discovery.
