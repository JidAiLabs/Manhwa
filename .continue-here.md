# .continue-here — 2026-07-02 — seam fix SHIPPED but merged 0 on real data; DIAGNOSIS DONE; awaiting user A/B/C decision

## ⏸ WHERE WE STOPPED (compaction point)
I asked the user: proceed with **(A) drop the dhash veto** + **(B) fix overlap-dedup**, hold **(C) the 3 legit near-dups** for their decision? **No answer yet — get it first thing next session.**

## 🔴 THE OPEN PROBLEM — cross_dup on Nano ch1 is 3 DIFFERENT problems (diagnosed from real data)
The seam-reconciliation tool shipped + deployed (`f2b1ae8`) but merged **0** panels on the real ch1 reprocess (cross_dup still 6). Per-pair diagnosis (from `manifest.scenes.json` geometry + dhash):

| pair | chunks | prev cond1 (px from edge) | dhash | category |
|---|---|---|---|---|
| p000015→p000016 | 2→3 | **6** (forced cut) | 29 | **① TRUE BISECTION** — the ONLY one |
| p000063→p000064 | 7→8 | 319 (gutter) | **5** | **② OVERLAP-DUP** (same panel in both chunks' 700px overlap band) |
| p000084→p000085 | 9→10 | 302 (gutter) | **7** | **② OVERLAP-DUP** |
| p000024→p000025 | 3→4 | 341 (gutter) | 25 | **③ DISTINCT near-dup** (real panels, artist progression) |
| p000043→p000044 | 5→6 | 198 (gutter) | 31 | **③ DISTINCT** (strike: charge→land) |
| p000054→p000055 | 6→7 | 185 (gutter) | 18 | **③ DISTINCT** (mist: sigh→resolve) |

**Fixes:**
- **(A)** The dhash veto (>20) in `tools/reconcile_seam_panels.py` **blocked the one true bisection** (its slices share only the 700px band ≈14% of a tall crop → whole-crop dhash 29). **DROP the veto** — geometry alone separates cleanly (bisection cond1=6px vs gutter cuts 71–341px across all 11 chunk seams). Update the veto unit test accordingly. Small change + test.
- **(B)** The 2 overlap-dups (dhash 5/7, ~300px from the edge) are TRUE duplicates from the 700px chunk overlap that `panels_to_scenes --dedupe-overlap` fails to catch cross-chunk → investigate/fix there (separate from the seam tool).
- **(C)** The 3 distinct near-dups are LEGIT panels under the user's "show every panel" — keep (and possibly exempt/downgrade cross_dup for them) or handle differently — **USER'S CALL**. Note: cross_dup is ERROR severity → not "spotless" → autopilot won't auto-advance (manual approval still works).
- After A+B: reset ch1 to `detected` again (clear approvals!), re-run, verify cross_dup 6→3 (or →0 per C), THEN voice+render for the user's listen.

## ✅ SHIPPED THIS SESSION (all on main, PUSHED to origin; Mini pulled = `f2b1ae8`)
1. **Niche narration** (`3702f15`, 13 commits): A/B/C/D niche classifier + catalog cols + always-on persona + niche registers via `manifest.series.json` (written by cli.cmd_run) + intensity recalibration + beat cap + flash_cut BLOCKING. Nano niche = **A+C** (stored on Mini, series_id 2).
2. **Per-panel 1:1 refactor** (`5a1f756` + docs `483bcd3`): merge-off → one panel = one line = one segment = one clip; image-aware duration (floor 2.0, cap 6, flash_cut<2.0); intensity→MLX per-clip exaggeration (affine remap [0.8,2.0]); `inject_missing_protected` KEPT (narration-less system cards only). **VERIFIED on ch1**: 112 lines→112 segments, durations 2.5–18.2s (0 <2.0), per-clip exag 0.35–0.85 (6 distinct), niche manifest present.
3. **Dashboard widget removal** (`27d3b78`): "recent chapters" table + dead `chapter_history` + its tests deleted (−214 lines). Dashboard restarted on Mini.
4. **Seam reconciliation** (`f2b1ae8`): `tools/reconcile_seam_panels.py` (detector+reassembly, 8 tests) + `_stage_scened` wiring + prep_qa `chunk_as_panel` exemption for `reconciled_seam:true`. Suite **1203 passed** (real baseline 1192 + 11; plan's 1198/1209 was stale after widget removal). Works in tests; **real-data gap = the dhash veto (A above)**.

## LIVE STATE on Mini
- ch1 (id **310**) = `scripted` (prepare job 20 done, post-seam-fix run): 116 scenes / 112 narration lines / QA: cross_dup 6 ERROR, **held_repeat 1 ERROR** (was WARN before — check why escalated), grounding_weak 23 WARN, ghost_text 1, sauce_density 1 (12/97 ≈12% vs ~25% target), reshow 2 INFO. **Renders HELD** (user decided: fix first, render once, judge everything together).
- ch2 (311), ch3 (312) = `detected`, approvals cleared, waiting until ch1 verifies.
- Old-code ch4–10 (rendered/scripted 2026-06-30) untouched — **must re-run before any bundle concat** (bundle 1 = ch1–10, teaser_state none). ~45–60 min/chapter serial. User aware.
- Worker pid fresh, dashboard restarted; heartbeat job only.

## OPEN ITEMS besides cross_dup
- **Persona wording** (parked, user's call): CINEMATIC_RULES still says "every line … a little arrogant"; agreed direction = always-on VOICE but SELECTIVE humor (~1-in-3/4 lines, matching the reference video); `recap_style.py` rule #4's 1-in-4 rationing should STAY (it matches the reference). sauce_density 12% confirms under-target.
- grounding_weak 23 (gemma names story entities not visually present — partly judge over-fire, partly embellishment).
- `system_card_unshown` (pre-existing): didn't fire on ch1's new runs; may still hit ch2/3 (historically parked them).
- Task list: #28 (run ch1–3 + verify) and #29 (seam fix) remain in_progress pending A/B/C.

## MINI OPS (verified this session)
- SSH: **`jidailabs@10.88.0.1`** (NOT anka/claw — earlier "blocked" was wrong username). Repo `/Users/jidailabs/repos/Manhwa`. `export PATH=/opt/homebrew/bin:$PATH` for ffmpeg.
- Enqueue: `.eval_venv/bin/python -c "from studio.catalog import db; from studio.dashboard import jobs; print(jobs.enqueue(db.connect('studio.db'),'prepare',series_id=2,chapter_id=310,priority=1))"` — worker claims it (serial GPU, `job` table, col `state`).
- **RESET LESSON (bit us once): a chapter reset MUST also `DELETE FROM approval WHERE chapter_id IN (...)`** — stale voice/render approvals make `_h_prepare` skip enqueueing voiceover (`not gates._has_approval` branches, worker.py:610/616) → chapter parks silently at scripted.
- Reset-to-detected file deletes (scoped to `ongoing/nano-machine/Chapter_N/`): rm -rf scenes scenes_clean tts render; rm manifest.{scenes,vision,sanitize,panels.understood,groups,story,beats,cast,script,series}.json heal_corrections.json render.plan*.json prep_qa.{json,html}. KEEP manifest.{stitch,panels,panels.expanded}.json + stitch_chunks/ + page JPGs.
- DB backups on Mini: latest `studio.db.bak-preseam-20260702-121336` (+ predeploy-1to1 earlier).
- Worker/dashboard restart only if worker.py/dashboard change: `launchctl kickstart -k gui/$(id -u)/com.originpower.{worker,dashboard}`. All this session's pipeline changes were tools/pipeline/cli = subprocess-fresh.

## NEXT SESSION SEQUENCE
1. Get the user's A/B/C answer (top of this file).
2. Implement A (drop veto in reconcile tool + fix its veto test) and B (cross-chunk overlap-dedup in panels_to_scenes) — TDD, suite green (baseline 1203).
3. Merge+push, Mini pull, reset ch1→detected (+ clear approvals), re-run prepare, verify cross_dup 6→3(or 0), no chunk_as_panel, reconciled_seam markers present.
4. User listens: approve → voiceover → render ch1; then ch2/3; then persona-density decision; then ch4–10 re-run for bundle consistency.

## Commit trailer: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
