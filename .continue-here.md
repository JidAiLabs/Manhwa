# Continue Here — Manhwa Recap Pipeline (post-compaction handoff)

**Last session:** built the full `studio/` pipeline (SP1) + narrative-quality overhaul, ran 2 chapters end-to-end to real narration, captured remaining visual-quality work as SP2.

## State (all on `main`, 41 commits, 170 tests passing)

- **`studio/` front-end works** — `add-series / fetch / run / qa / status` CLI over a SQLite catalog. See CLAUDE.md "studio/ — the front-end".
- **Live-proven end to end:** ORV ch1 (32 scenes→12 shots) and Nano Machine ch1 (→39 shots) both processed `fetch → stitch → YOLO → scenes → Vision OCR → Gemini beats → OpenAI script`. Status `voiced_failed` for both (only because no ElevenLabs key). QA reports: `ongoing/omniscient-reader/Episode_1/qa_report.html`, `ongoing/nano-machine/Chapter_1/qa_report.html`.
- **Narrative quality overhaul DONE** (R2 no-OCR-echo/no-repeat, R3 cross-section continuity, R4 manhwa jargon, flashback handling) in `tools/script_expander.py`. Nano sample: vivid, 15× jargon, ~10 min (in target), ~0 OCR noise.
- **Creds:** `keys/creds.env` has a valid `OPENAI_API_KEY` + `GOOGLE_CLOUD_PROJECT`. Beats use the `keys/gcp-vision.json` SA key against its OWN project (no gcloud). **ElevenLabs NOT added yet** — needed for `voiced → render.plan.json → Blender`.
- **Env:** use `.eval_venv/bin/python` (has all deps). `gcloud` is NOT installed (don't rely on it).

## Key learnings (don't re-derive)
- Source sites churn (Reaper died, Bato domain off, Flame forbidden) → adapters are config-driven/disposable.
- The trained YOLO replaces Gemini panel detection; Google Vision OCR still needed for *reading text* (YOLO gives boxes, not words).
- ORV ch1's "VIEWS: 1" narration is **story, not OCR noise** (it's a webtoon-with-1-reader plot) — a misleading QA example. Use Nano Machine as the fair test.
- `studio run`/CLI is authoritative for creds (loads creds.env, overrides stale shell env). Running `tools/*.py` directly needs `PYTHONPATH=.` and won't load creds.env.

## NEXT: SP2 — Scene & Bubble Quality
Spec: **`docs/plans/specs/2026-06-09-scene-bubble-quality-design.md`** (captures all QA findings w/ real examples as acceptance criteria). Build order:
1. **Over-segmentation** (Nano: 24 pages → 116 scenes ≈ 5/page; too choppy) — tune `panels_to_scenes` gutter-split.
2. **Text-only bubble merge** (p20 "PEASANT BLOOD…" became its own scene).
3. **Bubble inpaint** (white **and** black bg) via `clean_panels_inpaint.py` → `clean_scenes/` + timeline `--prefer-clean`.
4. **Bubble-type → narration mode** (jagged=internal "he thinks", smooth=spoken) via Gemini beats prompt.
5. **Split/diptych action panels** kept as one moment.
6. **Emotion-aware pacing** (intense=fast cuts, quiet=long holds) using beats `mood_words`.

DONE in SP1's scope already: overlap/containment dedup (`--dedupe-overlap`), min-3.5s/picture, dedup/skip-blank/trim flags.

**To resume SP2:** read the spec, then implement task-by-task with TDD, verifying against the real `ongoing/nano-machine/Chapter_1/` crops. Re-run `studio run 2 --chapters 1` to validate.

## To finish a full video (needs user action)
Add `ELEVENLABS_API_KEY` + `ELEVENLABS_VOICE_ID` to `keys/creds.env`, then `studio run 1 --chapters 1` → `render.plan.json` → `blender --background --python tools/blender_vse_from_plan.py -- --plan … --scene-dir … --out-blend …`.
