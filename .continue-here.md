# Continue here — 2026-06-24 (data-integrity sweep + autonomous recovery)

## What was broken (user kept finding it by eye) and why
The pipeline had **no panel-detection integrity check**, so under-segmented chapters
shipped QA-green. Three distinct failure modes, all now fixed:

1. **asura fetch** — page regex matched only `.webp` and couldn't read hash/`_pN`
   filenames → chapters fetched 3 of 5 pages (ch25) or failed entirely (ch28-52).
2. **stitch monster-chunk** — the final remainder was uncapped (only JPEG-safe 65k),
   so a 60k-px gutterless chunk reached YOLO, got downscaled, and lost nearly every
   panel → a whole chunk rendered as one thin strip ("chunk-as-panel", ch28/38).
3. **QA blind spot** — prep_qa flagged a chunk-as-panel only as INFO, never blocked.

## Deployed (main, pulled to Mini, worker + dashboard restarted)
- `a6f8256` asura mixed-ext regex + page-contiguity gate
- `fbedb89` asura hash/`_pN`/mixed filename schemes (reading-order extraction)
- `e1e735f` dashboard: sync refresh button + labeled `prep·voice·done` counts
- `0d28eea` stitch chunk-height cap + dashboard latest-attempt "!" marker
- `ca8ff71` prep_qa `chunk_as_panel` BLOCKING gate + YOLO re-tile guard + credits-card demotion
- `32ab65b` (audit #1+#2) normalize_into clears stale pages on re-fetch; system-card-mislabeled-chrome rescue
- `06ee886` (audit #6+#9) atomic page write; system_card_unshown blocking
- `bd1dd2c` (alignment audit) rebuild clears stale audio + voice/render approvals — fixes the rebuild-ships-gen1-audio-under-gen2-captions bug

**2nd ultracode audit (w04i4gei3, narration↔audio↔video alignment — the dimension the
1st audit missed):** 1 confirmed of 13 raw = the rebuild stale-audio bug (FIXED, bd1dd2c).
Its data-scan found NO real stale-video rendered chapters — completed videos are aligned.
ch36-style mismatches are only the in-flight re-process transient.

**Proven on real data:** ch28 32→82 panels, ch36 56→81 (no strip), ch20 byte-identical
(no regression). Full suite 1004 passing.

## Recovery in motion (worker grinding autonomously, chapter order)
- **11 under-detection chapters** reset to `downloaded` + re-queued: ch24,25,28,30,31,
  32,33,34,36,38,53. Verified re-stitching with the cap (tallest chunks now ~16k).
- The earlier **fetch-bug dead-letters** (ch4,5,28,30-52…) re-queued.
- Each re-processes with stitch-cap + re-tile guard → full panels; the gate blocks
  any residual at QA. Priority = chapter number, so they run ahead of the backlog.
- The 250-chapter backlog (ch59+) now fetches + detects correctly as it processes;
  the gate self-catches anything new.

## Ultracode audit RESULTS (workflow w6spbil6l — 38 agents, 10 confirmed of 30 raw)
Full detail: /private/tmp/claude-501/-Users-anka-repos-Manhwa/e265e699-aec9-4921-a594-044a48401ebe/tasks/w6spbil6l.output

FIXED (commits 32ab65b + 06ee886): **#1** stale-pages-on-refetch, **#2** system-card-
mislabeled-chrome-drop, **#6** atomic page write, **#9** system_card_unshown blocking.

ALL 6 REMAINING NOW FIXED — commit `279dc54` (re-derived adversarially via workflow
w91h0sgg7; every fix verified, none refuted; +10 tests; full suite 1014 passed).
⚠ DEPLOY PENDING: worker.py changed → each factory host's WORKER daemon must be
restarted (kickstart -k) for #5 + the #4 doc to take effect. tools/* + prep_qa.py +
studio/sources|detect are subprocesses → fresh on `git pull` (Mini) / already present
(Air). I could NOT reach the Mini over SSH (key rejected; did not guess creds) and the
Air worker label isn't currently loaded → USER deploys (commands at bottom of file).

- **#3 A/V drift — FIXED (latent path).** blender_vse_from_plan.py: added
  rel_seconds_to_frames (floors to 0), re-anchor last_used_frame_end_by_channel[FG_CH]
  = shot_fs-1 per shot, line 450 uses the floored converter. ⭐ KEY FINDING: PRODUCTION
  RENDERS VIA REMOTION (`npx remotion render`, worker.py:633/720), NOT Blender
  (pipeline.py:416 = manual follow step). Verified Remotion seats every shot AND its
  audio at absolute start_sec (RecapVideo.tsx:13, Shot.tsx:28, toStartFrame(0)=0) → NO
  drift in shipped video. So #3 only fixes the manual Blender path; not a prod bug.
- **#4 visual_loop — RESOLVED: deliberately NOT promoted** (documented in worker.py).
  cap_repeats_with_holds (cap=2) hard-caps non-exempt panels at 2 fresh cuts (3rd→held
  →filtered from montage by_file), so visual_loop can ONLY fire on legit far-apart
  EXEMPT-card recurrence — promoting it false-blocks and catches nothing the already-
  critical montage_degenerate/chunk_as_panel miss. The audit's "promote it" rec was
  WRONG; overrode it after reading cap_repeats_with_holds. Stays ERROR (autopilot parks
  for human) but never hard-blocks manual approval.
- **#5 heal silent-green — FIXED.** _heal_visual_drops returns the visual-droppable
  codes it could NOT remove (>25% cap, or no-op drop on a sole-cut unique blank);
  _h_prepare blocks on `codes & stuck_visual`. Stays green when the drop succeeds.
- **#6 elftoon atomic write — FIXED** (tmp + os.replace, matches asura). gallerydl
  already safe (normalize_into wipes+rewrites every call).
- **#7 low_page_count WARN — FIXED.** prep_qa.page_floor_flags: cross-chapter series-
  median page floor (0.45×, self-discovered from sibling ep_dirs' manifest.stitch.json,
  needs ≥5 siblings). Advisory only — catches truncated opaque-name fetches the numeric
  contiguity gate can't see; conservative floor so legit-short chapters never trip it.
- **#8 effect_only over-drop — FIXED.** Broadened _CONCRETE_NOUNS (battlefield, ruins,
  smoke, silhouette, …). Additive: only flips DROP→KEEP. (Skipped the empty-description
  tightening — touches a pinned test; raise as a separate decision if wanted.)
- **#10 element-snap overlap — FIXED.** snap_panels_to_elements clamps grown edges to
  the gutter midpoint vs original neighbour boxes → no overlapping crops.

## Other known render-polish (pre-audit, not started)
- **same-image-3×-pan** — one image over N segments restarts the left-pan each time;
  fix = one continuous pan (or static) across the combined duration. (Related to #4.)
- **credits-card gate is PARTIAL** — catches the ~8 OCR-readable credit chapters;
  stylized title cards (OCR misses them) need a layout/template signal.

## DEPLOY `279dc54` (run these — I couldn't reach the Mini / Air worker not loaded)
Code is on origin/main + tested green. worker.py changed → restart the WORKER daemon
per host (the change is in the long-running process; subprocesses are fresh on pull).
Dashboard was NOT touched → no dashboard restart.
- **Mini** (10.88.0.1): `cd ~/repos/Manhwa && git pull` then
  `launchctl kickstart -k gui/$(id -u)/com.originpower.worker`  (kickstart uses the
  plist PATH so ffmpeg/homebrew stays on PATH — do NOT relaunch the worker by hand
  over bare SSH, that drops /opt/homebrew/bin).
- **Air** (this host): code already present (committed here). The worker label is NOT
  currently loaded (`launchctl list | grep originpower` shows dashboard/refresh/ollama
  only). If the Air factory should be running:
  `launchctl kickstart -k gui/$(id -u)/com.originpower.worker` (or bootstrap the plist
  if it was bootout'd). If you intentionally have the Air worker off, nothing to do —
  it'll pick up the new code whenever you next start it.
- Restarting a worker SIGKILLs any in-flight job; requeue_orphans re-queues it on boot,
  so it's safe but interrupts one chapter per host.

## Audit signal (for future "missing images")
A scene/panel taller than **8000px** = a whole chunk (legit panels max ~5.2k). Use
absolute height, NOT h/w (over-fires on legit tall splashes). Audit:
`max chunk_h>22000` (stitch manifest) OR `scene h>8000` (scenes manifest).
