# .continue-here — 2026-07-02 evening — cross_dup RESOLVED, ch1 GREEN; ch2/3 prepares running

## ✅ WHERE WE ARE
**cross_dup is fully resolved and VERIFIED on real data** — Nano ch1 (id 310) re-ran to **0 QA ERRORs** (job 26, 3 min, no heal spin). The user's C decision landed mid-session: near-dups ARE duplicates → auto-drop the later copy, its narration HOLDS on the kept panel (screen shows one panel carrying both lines; same-image merge = one slow pan). ch1 = `scripted`, **renders + voice HELD** for the user's listen.

## 🔧 SHIPPED TODAY (all on main, pushed; Mini = `d280c3b`; suite 1212)
1. `8eb2fcc` fix A — dhash veto dropped from `reconcile_seam_panels.py`; real bisection p15/16 now merges (`reconciled_seam`, h=1416).
2. `9a8e4b6` fix B — `panels_to_scenes.same_strip_overlap()`: adjacent chunks + true-global overlap ≥50% → hamming alone dedupes (ratio/area skipped). Killed the 2 overlap-band dups (116→113 scenes).
3. `5c3b645` — `cross_dup` → worker `_VISUAL_DROPPABLE` (WORKER change = daemon restart, done).
4. `047ec39` — **1:1 regression fix**: sole-cut junk segments substitute a held story-adjacent panel (`substitute_garbage_sole_cuts` with junk@cov1.0). Before this, ALL manual/dashboard/heal drops were silent no-ops under 1:1.
5. `d280c3b` — junk outranks rich-panel exemptions; system exception keyed on stamped `system_files` (panel_kind), NOT scene_dims `sys` (pixel YOLO, trips on SFX — all 3 STORY dups carried sys:True).
Also: old-code `stage_run` history for ch1–10 purged (user-approved) — dashboard tile now honest (prep/voice counters were reading this ledger, not status); ETA medians reseed from new runs. ch4–10 reset to `detected` + approvals cleared + artifacts deleted.

## ▶ IN FLIGHT (check first!)
- **ch2 (311) job 28 + ch3 (312) job 29 prepares queued/running** (serial GPU, ~35–90 min each). Monitor script: scratchpad `monitor_ch23.sh`. On completion: verify QA like ch1 (expect cross_dup auto-dropped or 0; watch for `system_card_unshown` — historically parked ch2/3!).

## NEXT SEQUENCE
1. ch2/3 finish → verify QA reports (ERROR-free or explainable) → tell the user all three are ready.
2. USER: listens/reviews ch1–3 narration on the dashboard → approve → voiceover (qwen-mlx) → render. RENDERS STAY HELD until they say go.
3. Then ch4–10 prepares (already at `detected`, clean) for bundle-1 consistency.
4. Parked user decisions: sauce_density ~11% vs ~25% target (persona wording change), grounding_weak 28 WARN (judge over-fire), name_ration 7>6.

## MINI OPS
- ssh `jidailabs@10.88.0.1`, repo `/Users/jidailabs/repos/Manhwa`, PATH=/opt/homebrew/bin for ffmpeg.
- **PULL BEFORE kickstart** (bit us: job 22 ran stale code; `jobs.cancel(con, id)` = graceful cancel, worker kills its own subprocess tree).
- Enqueue: `jobs.enqueue(con,'prepare',series_id=2,chapter_id=NNN,priority=1)`.
- Resets: status + `DELETE FROM approval WHERE chapter_id` + scoped file deletes + (full-clean only) `stage_run` history.
- DB backups: latest `studio.db.bak-prefixab-20260702-151130`.

## Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
