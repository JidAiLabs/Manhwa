# .continue-here — 2026-07-03 04:30 — ADAPTIVE FLOW NARRATION SHIPPED; ch1 review copy RENDERED, awaiting user listen

## ⏸ WHERE WE ARE
**ch1 (310) = `rendered`** with the new flow narration — the user's review copy: `ongoing/nano-machine/Chapter_1/render/segment_both.mp4` (6:37, 80MB, rendered 04:30) + dashboard `http://10.88.0.1:8170/chapter/310`. Push ping sent (terminal only). **Waiting on the user's listen verdict.** ch2 (311) = scripted (old per-panel narration, stale), ch3 (312) = detected. Autopilot OFF for Nano. Renders beyond this review copy stay user-gated.

## ✅ SHIPPED (main = `d953fe4`, Mini deployed same; suite 1329)
**Adaptive flow narration** (spec `docs/plans/specs/2026-07-02-adaptive-flow-narration-design.md`, plan `docs/plans/2026-07-02-adaptive-flow-narration.md`): beats emit `segments[] = [{span: 1-4 panels, line}]`; ONE clip per segment; planner paces spans via existing multi_cut (≥2s floor); all consumers via `tools/beats_segments.py` helpers; QA cover checks (`panel_uncovered`/`panel_double_covered`); span-pinned heal; `[narration].segmentation` escape hatch (`per_panel` byte-compatible). Built via 3 reviewed chunks + whole-impl review (11 commits merged FF `4924170`).

**5 hot fixes from real-gemma rollout** (each verified on a real ch1 run):
1. `e20a068` anti-parrot prompt + DEFAULT TO FLOW + all-singleton nudge re-ask (run 1: valid garbage — 22/22 singleton caption echoes).
2. `902b502` budget ceiling 6→10s/panel (run 1 fallout: 18/21 beats fell back; the ceiling rejected money-shot holds).
3. `a1d31c7` egregious-only gates (1s/15s) + `auto_repair_segments` (extract in-span system cards solo, insert skipped panels as pads — prose preserved; run 2: 13/21 fallbacks decomposed).
4. `c0d5479` slimmed adaptive prompt ~40% (gemma 420s stall at num_ctx 12288 — the 8192-overflow zone; job 34 died, auto-retry job 35 landed).
5. `d953fe4` **fallback-under-pin guard + corrections speak segments** (the big one: corrections regen told gemma to fix `narration` not segments → malformed → fallback pads; all-singleton pins MATCHED the pad fallback → 6 healed beats poisoned with "The moment holds."). Pinned regen now keeps the previous beat on fallback.

**Final ch1 numbers** (job 36 fresh beats + surgical heal + job 37 rescript + job 38 voice + job 39 render): 21 beats, 83 segments, span mix {1:56, 2:26, 3:1}, 4 fallbacks, 3 residual weak lines (g0015 caption-ish, g0017/g0021 text-display), QA = 1 cosmetic ERROR (shot_description) + WARNs (grounding_weak 21, sauce_density ~low — parked persona-density decision).

## NEXT
1. **User listens** → verdict drives: (a) approve → run ch2/ch3 (reset ch2 to grouped first — its script is OLD per-panel!), then ch4–10; (b) tweaks → iterate on ch1 (prompt/density levers documented above).
2. Parked: sauce_density ~25% target (persona wording), grounding_weak judge over-fire, narration-level ch1 hook (Prompt-3 analog — teaser remains the video cold open), "compression/atmosphere-cut" rule from the user's prompt system (not yet an explicit writer rule).
3. Tech debt noted: `_base_scene` vs `parent_scene` duplication in prep_qa (pre-existing); `regenerated` stat overcounts pin-rejected regens; heal-loop still re-narrates unhealable flags (convergence guard caps it).

## OPS NOTES (hard-won tonight)
- Heal regens: worker's heal loop OVERWRITES `heal_corrections.json` from QA flags — manual corrections must run the tool directly (argv in `logs/jobs/35-prepare.log`), then a prepare for rescript via staleness heal.
- ssh single-quote wrapper: NO apostrophes/single quotes in embedded strings (bit twice: `\"` SyntaxError + `character(s)` glob).
- Background bash monitors kept getting killed → use the `Monitor` tool (survives; covers all terminal states).
- Review-copy gates: `gates.approve(con, "voice"/"render", chapter_id=..., note="review-copy...")` then enqueue voiceover/render_segment (autopilot off).
- gemma stalls: watch `num_ctx 12288` retries in beats logs; the 420s watchdog + worker auto-retry (3 attempts, resume) recovers.

## Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
