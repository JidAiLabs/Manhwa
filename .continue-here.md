# .continue-here — 2026-07-06 — DEPENDENCY-AUTHORITY REFACTOR SHIPPED (all 3 phases)

## WHAT THIS IS
Executed the approved plan `/Users/anka/.claude/plans/full-diagnostic-result-no-velvety-kay.md`
— the full dependency-model refactor that ends the month of whack-a-mole (verified root cause:
no single authority for "which downstream artifacts/approvals became invalid"; state split
across DB/FS with per-consumer copies of freshness/rewind/QA-policy/naming/cache-keys).
Method: subagent-driven dev — every task independently spec+quality reviewed, fix loops until
approved. Ledger: `.superpowers/sdd/progress.md`. Suite 1413 → **1554 passing**.

## SHIPPED + DEPLOYED (both hosts; Mini + Air daemons kickstarted per phase)
- **Phase 0 (safety)**: chapter lease in claim_next (one running job per chapter, skip-not-block)
  + enqueue dedupe (adopts more-urgent priority); child pgid persisted + identity-checked
  orphan reap BEFORE requeue; **QA fail-closed** (QAVerdict: missing/corrupt/stale report =
  blocking qa_report_invalid; autopilot consumes in-memory verdict); structural codes
  cut_gap/panel_uncovered/panel_double_covered/audio_failed now BLOCK (escape hatch env
  STUDIO_QA_NONBLOCKING=csv); tts_failed silence clips flagged (audio_failed) + never reused;
  sanitize freshness backstop at voiced + heal re-sanitizes; Cut.tsx tall yields to split2
  (both halves render); segment naming centralized (paths.SEGMENT_MP4 + find_segment_mp4,
  dead branding=none/single.mp4 branch deleted).
- **Phase 1 (the authority)**: `studio/deps.py` single dependency table → freshness, rewind
  delete-lists, reconcile marker validation (parse-validated + _VALID_CACHE memo), pipeline
  skip-iff-fresh ALL DERIVE from it. `tools/manifest_io.py` atomic writes + `_meta` input-sha
  stamps across all churn-core writers; in-place rewriters (sanitize/punchup) carry upstream
  provenance. panel_understand write-back reordered (mtime inversion fixed → understood←vision
  edge live, sha_only). Approvals content-bound (approval.content_sha, gates.gate_sha,
  ensure_approval unwedges auto-advance); reconcile backfills legacy rows once then clears
  drift. **Render pinned to QA'd plan sha** (match → skip render_prep; drift → fail loud).
- **Phase 2 (provenance caches + teaser)**: TTS reuse invalidates on backend/voice_ref(_sha)
  change (both tools); panel_understand resume keys on scene_sha + PROMPT_VERSION (prompt
  change = bump const — the delete-understood.json gotcha is DEAD); reconcile voice-gate
  backfill; teaser fully wired (spoiler payoff_tail_frac=0.20 ON, min/max panels,
  --max-seconds, backend dispatch incl. elevenlabs, --speed, sanitize-gated with marker
  freshness guard); gates.teaser_allowed deleted.
- **Phase 3**: cross-stage tests (two-lane lease race, reap-before-requeue order, corrupt-marker
  matrix, provenance e2e chains, dispatch-audit, report-shape guard, mtime boundary, conftest
  hermeticity vs ambient API keys) — commit 263d930.

## OPERATIONAL NOTES (read before the next batch run)
- **One-time cost**: every pre-refactor `understood.json` is legacy under the new resume keys →
  a chapter's NEXT grouped-stage run re-understands fully once (Gemma time, no API cost).
- **Expected on dashboards**: TRUE-positive stale banners on half-rewound chapters (new
  freshness edges see real staleness the old code missed). Red banner = re-run the stage.
- Chapters that used to slip through with cut_gap/panel_uncovered/panel_double_covered now
  PARK at QA. That is the designed behavior; STUDIO_QA_NONBLOCKING demotes per-code if triage
  demands.
- Live/E2E testing happens on the MINI ONLY (user rule 2026-07-06); Air = dev seat.

## PRE-REFACTOR WIPE (2026-07-06, user-directed: "won't approve anything pre-refactor")
- ALL pre-refactor pipeline products deleted on BOTH hosts: every chapter beyond `detected`
  rewound to it (Mini: ORV ep0 + nano ch1/ch2 from scripted; Air: tutorial-tower ch1/ch2),
  approvals table EMPTY, bundle teaser_state reset, finished job history + job logs deleted,
  daemon logs truncated, dist/bundle_* removed. DB backups: studio.db.bak-prerefactor-wipe-*
  on each host. Sources + stitch + panel boxes KEPT (detection code unchanged by the refactor).
- NOTHING is pending approval. First runs under the new machinery start from a clean slate:
  enqueue prepare on the dashboard (or studio run) when ready — everything from scenes/vision
  onward regenerates under the new provenance/QA rules.

## NEXT (if a session picks this up)
- Final whole-branch review may still be in flight (check `.superpowers/sdd/progress.md`
  ledger tail + `git log`). After it: last merge to main + deploy both hosts + daemon restart.
- Deferred decision parked for that review: scripted-rewind deletes plan files while
  _STAGES_BEYOND["scripted"] keeps planned/prepped stage_run rows (reconcile self-heals;
  decide whether to align).

## GOTCHAS (unchanged)
- Mini: ssh jidailabs@100.87.229.28 (Tailscale — if unreachable, check Tailscale is RUNNING on
  the Air first: /Applications/Tailscale.app/Contents/MacOS/Tailscale up). Dashboard
  100.87.229.28:8170 (token in plist). PATH=/opt/homebrew/bin:$PATH for remote launchctl.
- Commit messages: NO backticks (zsh eats them).
- worker.py/dashboard changes need daemon kickstart on both hosts; tools/pipeline are
  subprocess-fresh on pull.
