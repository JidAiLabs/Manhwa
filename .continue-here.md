# Continue Here — 2026-06-16 (post visual-fixes + narration recovery)

## TL;DR
HEAD `b560e46` (Air + Mini synced). All 3 reviewed chapters are QA-GREEN with visuals
the user confirmed "very good". Narration for ORV is recovered + LOCKED. **Next: build
the QA-eyes → auto-heal narration upgrade, then a current-vs-auto-heal comparison across
the first episode of all 4 manhwa, all produced via the dashboard, then a written report.**

## DONE this session (all committed + deployed)
- `cd64fe9` render: blank speech-bubble text **clipped flush to the panel edge** (IE p000111
  "JOINING OUR CLASS." — detector found the bubble at 0.875 but the eroded interior couldn't
  reach edge-flush text; now trusts the box+OCR for edge bubbles). No-op for non-clipped.
- `0f198f3` panels: **rescue in-world screens mis-classified as chrome** — a chrome panel with a
  CONFIDENT (≥0.70) COMPACT (≤0.40 area) speech balloon over its dialogue → story (ORV p000003
  "IT'S A MASTERPIECE!" episode list). Deterministic, no hardcoded text. In `panel_understand`.
- `9951718`+`b560e46` render: **in-world screen keeps its screen text** (route to the DOCUMENT
  treatment via the `subjects=['an in-world screen']` marker on vision_item), **even in speech
  mode** — the voiced comment bubble still blanks, the episode list behind it stays. (Without
  this the rescued panel rendered gutted.)
- **ORV narration recovered + LOCKED.** I had re-run ORV full (visioned→re-narrate) to add the
  masterpiece panel → that re-rolled ALL narration (temp 0.7 non-determinism) and overwrote the
  user's approved take (NOT backed up; `ongoing/` gitignored). Recovery: generated one fresh
  roll (job 21), showed A(job16)/B(fresh) per beat, user picked a composite (selection only — NO
  prompt change, NO hand-edited text), assembled → `manifest.beats.json`, froze with
  `.narration_keepbase`. Backups on Mini: `manifest.beats.{job16-BAK,fresh-BAK}.json`.

## KEY LESSONS (see memory `narration-recovery-keepbase-autoheal`)
1. **NEVER full-reset a chapter to fix a VISUAL.** Visual fixes live in render_prep/panel_understand
   and only need a render_prep re-run with status left at `scripted` (rm `render.plan*`,`prep_qa.json`,
   enqueue prepare) — narration untouched. Resetting to visioned/grouped re-rolls narration.
2. **Back up `manifest.beats.json` before ANY narration op** (gitignored → otherwise unrecoverable).
3. **Lock approved narration with `.narration_keepbase`** (beated reuses beats verbatim; punchup still runs).
4. Narration is non-deterministic by design (punchup temp 0.7 / beats temp>0): same prompt → different
   wording each run. "A/B pick" = choosing between two real generations, never editing text or prompts.

## 4 manhwa (first episode each), current state on Mini
- ch1   Omniscient Reader  Ep0 — `scripted`, narration LOCKED (composite), GREEN
- ch310 Nano Machine       Ch1 — `scripted`, GREEN (narration not yet locked)
- ch625 Infinite Evolution Ch1 — `scripted`, GREEN (narration not yet locked)
- ch724 I'm The Max Level Newbie Ep1 — `visioned` (UNTOUCHED — needs a baseline run)

## NEXT — the user's 4-phase plan
1. ✅ Commit checkpoint (445519c).
2. ✅ **QA-eyes → auto-heal BUILT + deployed** (off by default `[heal].semantic=false`; env
   `STUDIO_SEMANTIC_HEAL=1` toggles). Commits: `f12172d` safeguard tool, `394a9b3` feature wiring,
   `e4b0724` montage-aware eyes. 619 tests pass.
   - `tools/narration_accept_better.py` — strictly-better safeguard: per healed group, judge OLD vs NEW
     against the panel, keep new ONLY if "B_better" (conservative; fail-safe keeps old). 6 unit tests.
   - `tools/prep_qa.py grounding_flags()` (`--semantic-heal`) — montage-aware grounding judge → HEALABLE
     `grounding_weak` WARN. `tools/narration_heal.py` heals it at any severity.
   - `studio/worker.py` — passes `--semantic-heal` when on; in `_regen_flagged` snapshots
     `manifest.beats.preheal.json` and runs accept_better AFTER punchup, before script.
   - **VALIDATED on Nano (job 23, flag on):** eyes caught real hallucinations ("invents a nano machine",
     "no one is yawning", "no wound but says blood pooling"); safeguard cycle 1 `kept_new=9 reverted=14`
     → improves-or-holds, never degrades. ERROR=0 throughout. WORKS + SAFE.
3. **Produce both versions via the DASHBOARD** — for each of the 4 first episodes a *current* (flag off)
   and an *auto-heal* (flag on) narration. HARNESS:
   - First **restore Nano current**: `cp ongoing/nano-machine/Chapter_1/manifest.beats.current-BAK.json
     manifest.beats.json` (job 23 partially healed it) and re-prepare flag-off if needed.
   - **Max-Level (ch724) is at `visioned`** → needs a current baseline prepare first (flag off).
   - "current" baselines exist for ORV(locked composite)/Nano/IE; snapshot each `beats.json` →
     `*.current-BAK.json`. ORV is keep-base LOCKED → its auto-heal == current (skips regen); fine, note it.
   - Auto-heal version: enable flag (`STUDIO_SEMANTIC_HEAL=1` in the worker env via `launchctl setenv` +
     kickstart, OR flip Mini `studio.toml semantic_heal=true` — config is read PER-JOB so no kickstart for
     the toml route; revert after), `rm render.plan*/prep_qa.json`, enqueue prepare. ~10-20 min each
     (montage eyes + heal cycles, bounded 4).
4. **Written report**: current vs auto-heal per chapter — which beats `grounding_weak` flagged, kept_new vs
     reverted (safeguard), and the before/after lines. Use `/tmp/compare_narration.py` pattern (old vs new
     by group_id). Expected story: eyes flag the embellished/invented lines, safeguard keeps only genuine
     fixes, ORV unchanged (locked).
   NOTE FROM VAL: the eyes are STRICT (flag any invented detail). Most regens aren't better → safeguard
   reverts them (safe but churny). If churn is a problem at scale, a refinement is to stop re-flagging a
   group once the safeguard has reverted it once (don't retry an unimprovable beat every cycle).

## MECHANICS
- Mini `jidailabs@10.88.0.1` (`~/repos/Manhwa`, `studio.db`). Air = control seat (WireGuard).
  Dashboard `http://10.88.0.1:8170` (token `STUDIO_DASH_TOKEN`). worker.py/dashboard/** change →
  `launchctl kickstart -k gui/$(id -u)/com.originpower.{worker,dashboard}`; tools/+pipeline.py fresh on pull.
- Deploy = commit→push→`git pull` on Mini (NEVER rsync — it blocks pull). Always keep both synced.
- Re-run only render_prep (no re-narrate): rm `render.plan*`,`prep_qa.json`, enqueue prepare (status stays scripted).
- Holistic QA that works: pull `render.plan.clean.json`, scp `scenes_clean/*.jpg`, eyeball panel↔narration.
