# Continue Here — 2026-06-15 (post understanding-first redesign)

## What shipped this session (all deployed to Mini @ HEAD `cbff620`, daemons restarted)

**The understanding-first pipeline redesign is COMPLETE** — replaced the dumb
position/gutter grouping (root cause of missing images / flat narration / wrong
grouping). See `[[understanding-first-pipeline]]` memory + spec
`docs/plans/specs/2026-06-15-recap-quality-root-cause-fix.md`. New flow:

1. **Pass 1** `tools/panel_understand.py` — describe every panel (full coverage).
2. **Pass 2** `tools/story_group.py` — group by understanding → story-beats +
   flashback tag (replaces `scene_group_builder.py`). `repair_to_shots` = coverage
   invariant.
3. **Pass 3** `gemini_narrative_pass.py` — narration LENGTH matches the panels
   (content-driven, not a fixed sentence count).
4. **Punchup** `narration_punchup.py` (cinematic) — LLM DECIDES cinematic/persona/
   both per line (no force, leans both); story captions stay VERBATIM.
5. **Planner** `timeline_planner.py` — paces every distinct panel UNDER the
   voiceover (no truncation, no stretch-into-silence, no music). Carries the
   `segment` flashback tag → plan item.
6. **Renderer** `remotion/src/Shot.tsx` — sepia+vignette flashback look.
7. **No intro** (`render_prep.insert_branding_items` forces intro_dur=0). No music.

Wired into the pipeline at `studio/pipeline.py::_stage_grouped` (Pass 1+2) and
`_stage_beated` (Pass 3 + punchup). Used automatically by dashboard "prepare → QA".

Earlier this session (also live): auto-heal-to-green (`_heal_to_green` +
`narration_heal.py`), prep_qa blank-crop validity gate (no sys/doc exemption),
chrome-scrub un-break, bundle-level title/desc (`publish_meta` job, Videos page),
series-level thumbnail job + Series-page approve UI, LANES gap fix.

## Verified on Omniscient Reader Ep0 (via /tmp scratch, NOT the full dashboard run)
6 flat beats → **11 story-beats**, flashback auto-detected + tagged, **24/27
panels shown** (3 are near-dup/caption frames, deduped), narration scales with
panels, pacing **3.0–10.7s/panel avg 4.7, zero flicker**, no intro/music/silence.
Full suite **593 passing**.

## NEXT SESSION — the user's plan (clear logs, run QA via dashboard)
The 4 first chapters (ch#1 ORV, #310 Nano, #625 IE, #724 Max-Level-Newbie) are at
status `scripted` from OLD-pipeline runs. To run the NEW pipeline fresh:
1. **Clean slate** (logs + DB job/stage history): clear `logs/jobs/`, and
   `DELETE FROM job; DELETE FROM stage_run;` in `studio.db`.
2. **Reset chapters to `visioned`** so the grouped stage re-runs understanding-
   first: `UPDATE chapter SET status='visioned' WHERE id IN (1,310,625,724);`
   (keep-base markers already removed — verified 0).
3. User runs **"prepare → QA"** per chapter on the dashboard (http://10.88.0.1:8170,
   token-gated). Each prepare now does: understand 34 panels → story_group →
   narrate(matches panels) → punchup(LLM-decided cinematic/persona) → verbatim
   script → estimate plan → render_prep → prep_qa(semantic) → **auto-heal to green**.
   Slower (~understanding is 34 Gemma calls) but it's the real end-to-end.
4. Review on the dashboard: story-beats, flashback, coverage, narration quality.
   Approve story → voiceover (Qwen) → render only when it looks right (user said
   NO voiceover until the visuals + narration are right).

## Wiring facts (so a deploy actually takes effect)
- `tools/*.py` + `studio/pipeline.py` run as SUBPROCESSES (`studio run`) → fresh on
  `git pull`, no restart needed.
- `studio/worker.py` + `studio/dashboard/**` are LONG-RUNNING daemons → need
  `launchctl kickstart -k gui/$(id -u)/com.originpower.{worker,dashboard}` after a
  pull that touches them. Done this session. See `[[deploy-worker-needs-daemon-restart]]`.

## Open / watch next run
- Confirm cinematic+persona quality + the LLM-decided balance on a FULL dashboard
  run (the /tmp verify showed it works; the live run is the real test).
- Confirm the flashback sepia/vignette VISUALLY once a chapter is rendered (logic
  wired + tsc-clean; not yet rendered to video — render needs voice).
- The 3 deduped panels per chapter = near-dups (the narrator's redundant call);
  user accepted captions-verbatim + dedup of near-identical frames.
