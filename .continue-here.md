# Continue here — narration/render fixes + Ch20 test (interrupted by power outage)

**Handover 2026-06-18 ~12:05. The Mini (jidailabs@10.88.0.1) is UNREACHABLE (power outage / WireGuard down — ssh exit 255 confirmed). The Ch20 test render was mid-flight (voicing). Resume once the Mini is back.**

## ⚡ STEP 0 — verify the Mini after power returns (do this FIRST)
```
ssh jidailabs@10.88.0.1 'uptime; launchctl print gui/$(id -u)/com.originpower.worker 2>/dev/null | grep -iE "state =|pid ="'
```
KEY RISK: the worker has RunAtLoad, so **if the Mini REBOOTED, the worker auto-restarted → Nano resumed on its own (un-paused), and Ch20 (status `grouped` from my test reset) was likely re-picked + re-narrated WITHOUT register-mode** (the worker env has no `STUDIO_NARRATION_REGISTER`), overwriting the test's register narration. Check:
```
ssh jidailabs@10.88.0.1 'cd ~/repos/Manhwa && .eval_venv/bin/python -c "import json,sqlite3; print(sqlite3.connect(chr(34)+\"studio.db\"+chr(34)).execute(\"SELECT status FROM chapter WHERE id=329\").fetchone()); b=json.load(open(\"ongoing/nano-machine/Chapter_20/manifest.beats.json\"))[\"beats\"]; print(\"register-stamped:\", sum(1 for x in b if x.get(\"register\")), len(b))"'
```
If register is gone / Ch20 reverted → just re-run the Ch20 test (procedure below).

## ✅ All 5 fixes COMMITTED + pushed to main (deploy to Mini = `git pull`)
| commit | what |
|---|---|
| `bad1308` | planner: never truncate narration (panel covers the full voiceover) |
| `7b1809a` | sanitizer NET + generator rules (tools/narration_sanitize.py, narration_denylist.json, narration_safe_rules.py) |
| `67b8371` | register-aware narration FAST/DEEP gears (gemini_narrative_pass.py `--register-mode`, OPT-IN, flag-off = no-op) |
| `5b3b044` | camera ends on a FACE, never the inpainted-blank bubble (timeline_planner per-cut motion + remotion Shot.tsx/plan.ts + blender_vse_from_plan.py) |
| `006fd55` | sanitize-pass WIRED into pipeline + auto-reframe (tools/narration_reframe.py, narration_sanitize_pass.py; DEFAULT-ON; cli.cmd_run now returns 1 on *_failed) |

Suite green at 656 passed / 1 skipped. Mini HEAD should become `006fd55` after pull (verify).

## State at the moment of the outage
- **Nano run was PAUSED** (worker bootout) for an isolated Ch20 test.
- **Ch20 test = a MANUAL run** (was pid 60261, `/tmp/test_ch20.py` on Mini), NOT a worker job (so it never showed on the job board). Progress: beats+script+sanitize DONE 09:53 — register 15/15 stamped (FAST 13 / DEEP 2), sanitize `ok=true 0 changes`, punchup off (FAST stayed terse), cast names kept ("Cheon Mu Geum"). Was VOICING (Qwen) when power died; render pending.
- **Orphaned jobs 479,480,724,734 were reset to `queued`** (re-process on worker restart).
- Test config is ENV-driven, no studio.toml edit: `STUDIO_NARRATION_REGISTER=1 STUDIO_PUNCHUP=off` (sanitize default-on).
- ollama: Mini np=4 (plist, persists reboot); Air np=2 + `gemma4:26b` num_ctx-8192 cap + `com.originpower.ollama-env` LaunchAgent.

## 🔧 "FIX EVERYTHING" — pending (user approved)
1. **Repeated-DEEP-opener BUG (found on Ch20):** g0012 AND g0013 (consecutive DEEP beats) both open with the IDENTICAL sentence *"The sting of battle fades into a memory far more haunting than any physical wound."* The register path's `previous_narration` isn't stopping consecutive-DEEP echo. FIX in `tools/gemini_narrative_pass.py` DEEP path — vary openers + verify the register narration call actually RECEIVES previous_narration (the subagent claimed it does; the echo says check it).
2. **Finish the Ch20 test render** → user JUDGES the video (the gate before any clean restart). Deliver it (scp Mini→Air, or dashboard `http://10.88.0.1:8170/chapter/329`).
3. **If good → CLEAN RESTART** all chapters with the fixes (register + sanitize + camera + punchup-off). Big multi-day re-run; user wants it AFTER judging the one chapter.
4. **Resume Nano** if still paused: `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.originpower.worker.plist`.
5. Deferred (not blocking): batch-TTS speedup (voiceover ~20min is the next lever — Qwen `generate_voice_clone` supports list batching, ~1.78x measured on short segs but touches the locked voice + determinism gate); a clean parallel-vs-1by1 throughput A/B.

## How to (re)run the Ch20 all-fixes test
Worker paused (`launchctl bootout gui/$(id -u)/com.originpower.worker`). `/tmp/test_ch20.py` may have survived; else recreate: set Ch20 (329) status `grouped`, delete `manifest.{beats,script,sanitize}.json` + `render.plan*.json` + `tts/` + `render/`, then `STUDIO_NARRATION_REGISTER=1 STUDIO_PUNCHUP=off .eval_venv/bin/python -m studio run 2 --chapters 20 --until planned`, then render (mirror worker.py `_h_render_segment`: render_prep.py then `npx remotion render src/index.ts RecapVideo <ep>/render/segment_both.mp4 --props=<ep>/render.plan.clean.json --public-dir=<ep> --concurrency=8 --crf=22` in remotion/). ~30-40 min isolated.

## Honest open notes (don't mislead next session)
- The "1h prepare" on the board was a FROZEN timer (paused job still ticking — NOT real work). The test's ~13min prep REUSED the cached understanding (reset to `grouped`), so NOT a full prep. Real full prep ≈ ~37min median. Parallel-vs-1by1 throughput is NOT yet cleanly measured (the two numbers we have aren't comparable).
- Validated this session: register FAST/DEEP gears (Ch20: fights 75w→~23w, the 2 memory beats deep); sanitizer 3-way replace/flag/block (fixed a false-block on "forced himself to stand" + the kill-swap-inside-flagged-span); camera face-end (direction proven, pixel amount render-only).

## Ops reminders
- Mini = jidailabs@10.88.0.1 (WireGuard 10.88.0.1). Deploy = commit→push→`git pull` on Mini (NEVER rsync). pipeline/cli/config/tools = subprocess-fresh on pull. worker.py + dashboard/** NOT changed this session (no kickstart needed) — but a reboot restarts them anyway.
- Config flags via env: STUDIO_NARRATION_REGISTER, STUDIO_PUNCHUP, STUDIO_NARRATION_SANITIZE. Dashboard token b67667af1ab05755d7e28dcef3a08c2c. Commits end `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Full transcript: /Users/anka/.claude/projects/-Users-anka-repos-Manhwa/84f9fa4e-c086-44e9-8e66-76c634a579f6.jsonl
