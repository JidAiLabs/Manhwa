# Continue here — 2026-09-21 (thumbnail follows the teaser: hook designs ranked from its window)

## ► 2026-09-21 SESSION (thumbnail hook from the teaser), on top of the 09-17 handover below

- **SHIPPED + DEPLOYED `3c8bfa0`** (Mini pulled, worker + dashboard kickstarted 20:1x, new PIDs,
  prepare 1787 re-queued itself and resumed). Owner's idea: the hook DESIGN comes from the example
  thumbnails, the best hook is selected AS PER THE TEASER. Plan: `~/.claude/plans/i-dont-understand-what-robust-moth.md`.
- **What changed.** `tools/publish_concept.py`: `teaser_montage` (planned/approved manifest, else the
  teaser's own selector over the first `[publish].auto_after_chapters` chapters; too few panels ->
  `claim_source: "digest"`), `rank_designs` (code ranks 2 of `thumbnail_styles.HOOK_DESIGNS` =
  `nametag` / `nametag_headline` / `system_window`), `window_card_lines` + `printable_card_line` +
  `_names_the_title` (the card QUOTES OCR from `manifest.vision.json`; one clean sentence of real
  words; never the licensed title), `assemble_package(design=, card_lines=)` writes `tags` (the
  second label), `--rank-designs` is free. `thumbnail_overlay.py`: `arrow_to`, `card`.
  `gates.THUMBNAIL_OPTIONS` = the closed set of design names; `_h_series_thumbnail` ranks once,
  clears `options/`, builds the two ranked designs. before/after = variant only (owner, 09-21).
- **The free dry run on the Mini caught what the unit tests could not** (fixtures had encoded my
  wrong assumption): system-card words are NOT in understood `dialogue` (empty on all 6 of ORV's
  teaser system panels) but in vision `ocr_clean`; raw OCR is unprintable (Cyrillic mirror-reads,
  `E0x`, blobs); ORV's only montage line named the series; card must come from the WINDOW (ORV: 16
  printable lines vs 1). Measured system panels per 10-panel montage: ORV 6, tower 2, Nano Machine
  1, Infinite Evolution 1 (a TV broadcast), Death Knight 0 -> `_MIN_SYSTEM_PANELS = 2`.
- **ORV would now build** `system_window` + `nametag_headline`; card: THE MAIN SCENARIO HAS ARRIVED. /
  YOU HAVE EXCEEDED THE TIME LIMIT. / TIME REMAINING: 7 MINUTES 32 SECONDS. **NOT generated yet —
  the paid run (~0.26 USD) is the owner's button** (Series page -> generate options; the job shares
  the gpu lane with ~36 queued prepares, so it may wait). First real look at arrow-on-lead + card.
- **NOT verified:** the Series page rendering the new options (dashboard redirects to /login for
  curl). If old `scene` / `before_after` cards still show, the dashboard did not reload.
- **OPEN:** (1) skill `manhwa-thumbnail` revise v2.0.0 -> v2.1.0 via skill-forge (forge.json has a
  `pendingNextRevision` from 09-17 to fold in) — install gate is the owner's; (2) rename
  `dist/series_1/teaser_src` -> `teaser` on the Mini (check `scenes/` symlinks are absolute, back
  up) so the approved manifest is read and the review card works; (3) `tech-debt.md` (new) lists
  the doomed first-video `plan_teaser` job, the stale re-plan manifest, the legacy
  `bundle.teaser_state` read, headline candidates not switchable, "SMILE CASH." passing the card
  filter; (4) `contrast_pair` / `roll_call` designs need a counterpart ref finder (NOT BUILT);
  (5) 3 series have no prepared chapters, so nothing can be ranked for them yet.
- Tests: 2654 passed, 1 skipped (`.eval_venv/bin/python -m pytest -q`) at `3c8bfa0`. The docs for
  it (CLAUDE.md, this file, `tech-debt.md`, `docs/plans/2026-09-05-thumbnail-cast-contract.md`)
  went in as the docs commit right after; docs only, no restart needed.

## ► 2026-09-17 SESSION (thumbnail + title), on top of the 09-15 handover below
- `main` @ `7098ec6`; Mini pulled; worker + dashboard **kickstarted** (worker.py + dashboard changed). Suite **2545 passed / 1 skipped**.
- `dc3872b` triptych removed everywhere (claimed "most common", counted 0 of 18 refs / 0 of the owner's 9) ·
  `series_thumbnail` builds `scene` + `before_after` into `dist/series_<id>/options/<name>/`, never live,
  `NonRetryableError` on failure · `POST /thumbnail/pick` copies one live + approves (note `picked option: <name>`) ·
  `assemble_package(styles=)` forces the layout · `thumbnail_build.art_text_words` refuses model-drawn text ·
  `refs_for_style` gives before_after an earlier-chapter lead-first ref (exit 2 if none) · `normalize_title`
  (` - Manhwa Recap`, no emoji, capitalised) · publish_meta reuses thumbnail words only after a pick.
- `7098ec6` split labels shrink to fit their half; the split badge goes upper-right.
- Skill `manhwa-thumbnail` **v2.0.0 installed** (forge state `~/.claude/_forge/manhwa-thumbnail/`); covers titles too.
- **ORV (series 1) options built** (job 1693, ~0.26 USD): `before_after` = office-worker Dokja on his phone →
  long-haired powered form, UNIMPORTANT SPECTATOR | THE SCRIPT MASTER (overlay redrawn after 7098ec6, no new image).
  `scene` = power_reveal, THE ONLY SURVIVOR, but the **lead is a faceless silhouette**: its refs are still the Ep306
  climax panels (lead in lightning / from behind / blurred). **Awaiting the owner's pick on the Series page.**
- `49d4b57` (owner: "why 3 images / label covers the MC's face / why long hair"): unpicked old live image hidden
  once options exist · corner labels fit a 40% column + stack on 2 lines · lead refs whose hair LENGTH contradicts
  the registry are dropped (cast_identity compares colour only; 390/2925 ORV portraits said long). Scene option
  re-overlaid (free). ORV refs now p000018, p000000, p000002 (p000001 long-haired close-up gone); p000002 is still
  shoulder-length messy hair described without a length. **Regenerating ORV options (0.26 USD) awaits the owner.**
  Skill doc changes for these are pending v2.0.1 (`~/.claude/_forge/manhwa-thumbnail/forge.json` pendingNextRevision).
- `30f1569` `6aa57dc` `946141d` `623c7fd`: the job-1694 regenerate still failed (after-half long hair from Ep306 refs;
  feat_object invented a stone hammer). Auto scene = power_reveal only; refs prefer panels stating the registry look
  (hair length + gender + colour, cast_identity._hair_colors). **Suggested reference panels shipped**: Series page
  "find reference panels" (free `thumbnail_refs` job, cpu lane) → 8 lead + 4 before tiles → tick → "generate options
  with these" (`POST /thumbnail/generate`, payload refs/before_ref → `publish_concept --refs/--before-ref`).
  ORV suggestions (job 1696) by eye: before 0–3 all Dokja; lead 2 (Ep96), 5 (Ep229) look like Dokja, lead 0 and 7
  are Lee Hyunsung (oracle mislabel), 4 is a tiny tall strip. **Owner to tick + generate.**
- `365a463` (owner: refs "wierd", no LEAD/BEFORE split, "use before / after not Observer / protagonist", "The Only
  Reader" not exciting): ONE set of up to 8 clear solo MC shots (1 subject, no dialogue, text <=3%, not a strip; ranked
  registry look > close-up > size; one per chapter); picks used as-is for both options; before_after ALWAYS labelled
  BEFORE|AFTER; scene label = 5 grounded "extreme on a ladder" candidates, `POST /thumbnail/label` redraws free.
  ORV suggestions (job 1697) by eye: Ep124 p000001 + Ep173 p000067 are clearly Dokja, Ep96 p000022 maybe; Ep100/132/139
  are other men. Clarity is fixed; IDENTITY is the weak link (cast_identity can't tell dark-haired young men apart,
  no face model). **Owner to tick + generate.**
- Open (skill "Not built yet" table): lead refs that hide the face (new, pending skill v2.0.1), arrows onto the
  labelled subject, a second contrast label, title number grounding, licensed-name check on titles,
  monster/opponent/object ref finder. After a pick, re-run `publish_meta` for bundles whose title should reuse the words.

## ► PREVIOUS HANDOVER

Older handovers: `git log -p -- .continue-here.md` (the 09-09 one described `dies_at`, REVERTED 09-10).

## ► STATE
- `main` @ the docs commit after `c091165`, Mini pulled. Every change this session is in `tools/` → **no daemon
  restart was needed**. Suite **2519 passed / 1 skipped**.
- Dashboard http://100.87.229.28:8170 · `ssh mini`.
- **Nothing of mine in flight.** Queue (Mini, 16:10 JST): Wimp Ch41 prepare running (job 1551), Ch42–46 queued
  (1552–1556), `series_thumbnail` 1601 queued.
- Rendered: omniscient-reader **309/309** · a-wimp **40/46** · nano-machine 2/329 · infinite-evolution 0/123 ·
  the-tutorial-tower 0/177 · i-rely-on-my-invincibility 0/196.

## ► SHIPPED 2026-09-13 → 09-15 (deploy order, all tools-only)
Writer + heal loop
- `1d0f7f8` heal guard: an empty ERROR set is not "no progress" (WARNs heal now)
- `6f9542e` a caption frame named in a span is dropped, not a whole-beat reject · `99e8c1d` a fallen-back regen is not re-punched
- `da648fc` fallback reason persisted (`segments_fallback`, `segments_fallback_reason`, `stats.segments_fallbacks`)
- `16654ea` `1877f92` `d415bb8` per-group panel-tag enum + `maxItems` (decode-time)
- `20971f6` A1 see a length-cut answer, retry once at a fitting window, never launder it through the formatter
- `39a4a5a` B1 honest fallback label · `025ab60` B2 the word cap counts the words the page prints
- `ece1b24` A2 ONE 16384 window for every writer call + over-cap input shrink (`STUDIO_BEATS_WINDOW`)
- `50c876b` system cards read aloud (≥50% of card words) + `system_card_unvoiced` WARN · `0d583a7` `ocr_looks_clipped` de-inflects

Voice
- `d2e5b65` a near-silent take is re-rolled like a crash; 0.25s absolute floor; a line ending in "..." gets 0.5s of
  silence (the synth still gets a full stop — "..." caused filler); cached clips topped up once (`pause_sec`)

Cards, silence, restore paths
- `96b17ec` `card_is_decoration` (Gemma saw no text AND OCR has no word → keep the writer's line) + card text that
  doesn't end a thought is never forced · prep_qa `unspeakable_line` (ERROR, healable, NOT blocking) ·
  `IMAGE_DWELL_BREATH_SEC=0.6` (a big panel outlasts its voice by ≤0.6s) · prep_qa `voice_gap` WARN (>1s)
- `a8b72f9` a system card + its own zoomed copy count as one card (glue vs solo-span contradiction made every attempt
  fall back) · the span-pin keep-guard refuses unspeakable card lines
- `0b79191` ledger `_each_named`: a story field listing two victims places both deaths · `usable_narration_line`
  requires `ends_terminal` (no guard keeps a line truncated_line blocks)
- `c091165` narration_accept_better floor (`old_unspeakable_card`) + the unpinned corrections keep-guards refuse
  unspeakable card lines

## ► MEASURED OUTCOME (closed; `python3 ~/scorecard.py 1511 1544` on the Mini)
Wimp Ch15–33 (minus Ch19) vs Ch1–13, same series:
| | before | after |
|---|---|---|
| targeted fallbacks (too fat / no shape / mid-sentence) | 4.0 / 100 beats | 0.4 |
| median prepare | 74 min (writer 33) | 50 min (writer 19.5) |
| model calls / beat | 1.83 | 1.10 |
| caption_unvoiced (first QA pass) | 7 | 1 |
| review errors left after heal | 0.9 / chapter | ~1.1 (de-duplicated) |
| narration per chapter | 7.2 min | 8.8 min — **owner accepted** |

## ► CHAPTER REPAIRS DONE (all rendered)
Wimp Ch1 (re-voiced for the "..." pause) · Ch5 ("Tada!" dead take) · Ch19 (hand-fixed "Physical." / "Traits and.")
· Ch21 ("Iix." → "The hourglass icon begins to turn.", 4 rounds) · Ch31 (re-planned: silent gaps 5→3, ≤1.6s) ·
ORV Ep298 (`refresh-facts 1 --chapters 299 --ledger-only`, dead_actor cleared) · ORV Ep299 ("The" → a sentence).

## ► OPEN / LATER (none blocking; owner rule: measure first, don't chase every finding)
1. A card heading split across two system panels (Ch19) should be read as ONE line — 1 of 1,726 card lines.
2. `sfx_voiced` scans `tts_paragraphs_v3` AND `script_paragraphs` → every flag counted twice; flagged the brand
   name "Wildflower".
3. Word list is web2 (Webster's 1934): lacks "box", "tada". NSSpellChecker (pyobjc, installed) knows them but
   ACCEPTS Roman numerals and rejects names — owner undecided.
4. `base_min` 2.5s still leaves ~1–1.6s after lines under 1.5s (1.1% of lines); `voice_gap` WARN lists them.
   Owner: leave unless it bothers him.
5. Heal runs up to 4 cycles on WARN-only chapters (`1d0f7f8`): 2/18 chapters hit the cap, WARNs −10%.
6. Phase 2: one context window fleet-wide (understanding still bumps 8192→12288/16384 on overflow).
7. Residual too-fat: verbose lines over wordless panels · pin-aware re-ask (11 cases) · guard for >12.9k-token prompts.
8. Older, NOT re-verified this session: cast-builder `gender` from pronouns; ORV thumbnail route (owner);
   `bundle` table empty until "create a new video"; visible_text by-construction audit; Tier 2 identity;
   `tools/narration_report.py` hard-codes Nano names.

## ► GOTCHAS EARNED
- **A new QA gate is only real once EVERY restore path shares its predicate** (span-pin keep, corrections keep,
  accept_better floor, fallback scrub) — Ch21 was healed and put back three times.
- Hand-editing beats: move `manifest.script.json` aside — `run_chapter` skips a stage whose marker EXISTS,
  freshness or not; the missing marker makes it rebuild `scripted` without re-running the writer.
- Re-voice = the dashboard's `post_revoice` (rewind `scripted` + clear render approval + ensure voice approval +
  voiceover with `{"auto_to": "video"}`). A voiceover enqueued without that payload stops after voicing.
- A prepare job re-plans, re-QAs and heals whatever the chapter status (even `rendered`).
- **Mini `git` is blocked by the Xcode license (since 2026-09-15 afternoon):** `/usr/bin/git` refuses with
  "You have not agreed to the Xcode license agreements". Nothing in the pipeline calls git, only deploys break.
  Workaround (no sudo): `DEVELOPER_DIR=/Library/Developer/CommandLineTools /usr/bin/git -C ~/repos/Manhwa pull --ff-only`.
  Real fix needs the owner's password on the Mini: `sudo xcodebuild -license accept`.
- ollama server log on the Mini is `/opt/homebrew/var/log/ollama.log` (the np plist's `/tmp/ollama-np.log` does not exist).
- `refresh-facts --chapters` keys on the DB number (ORV Ep298 = 299).
- My probe runs on the GPU slowed Ch14's understanding 3.5× — never probe during a measurement.
- Stale-file sweep done 2026-09-15 (owner-approved): deleted `qa_bench3.py`, `roboflow.rtf`, the Jun-18 Ch20
  report; on the Mini `husk_sweep.py` + `revoice_sweep.py` (both held the dashboard token in plain text — the
  token itself was NOT rotated), `orv-repair.json`, `~/generate_image.py`, and all session scratch.
  `docs/plans/2026-09-05-thumbnail-cast-contract.md` was committed (`40864a7`). Only `.claude/` is untracked locally;
  the Mini keeps `.mlx_venv/ .mlx_vlm_venv/ dist/ eval/` (runtime, not stale). `~/scorecard.py` stays (memory uses it).
