# Continue Here — Manhwa pipeline (2026-06-11, QA-first round DONE)

**State: prep-QA instrument built, all 3 chapters scanned CLEAN (0 ERRORs). NO renders started (per directive). Waiting on two user decisions (below).**

## What exists now
- `tools/prep_qa.py` — pre-render QA scanner: scans `render.plan.clean.json` + `scenes_clean/` per shown cut. Flags: husk, dead_box_leak, ghost_text, visible_text (glyph-aware), binary_card, stale_dims, extreme_tall, chrome_leak, doc_flag_missing, chrome_narration, ocr_echo (visible-text-only), plan integrity (missing file/dims/audio, flash/repeat cuts, cold-open, branding). Console + JSON + self-contained HTML w/ thumbnails at `<episode>/prep_qa.{json,html}`. Exit 1 on ERROR.
  Run: `$V tools/prep_qa.py --episode-dir ongoing/<series>/<chapter> --series-title "<Title>"`
- Fix wave it drove (commits 05b0eab + 3748cea, suite 367 green): dead-box recrop wired (band midtone-validated), residue sweep + orphan-word blanking in cleaner, doc_like discriminator (speech-shaped boxes; mixed panels stay docs; doc panels blank speech bubbles only), substitute_garbage_sole_cuts (chrome/husk sole cuts → nearest kept story panel), watermark-vs-cover chrome disambiguation (hard-hit count + midtone via needs_image_stats, shared with group builder + prep gate), whole-panel recoverability fallback @0.08, scene_dims sys/blanked metadata.

## Final scan state (v7, all exit 0)
- Nano Machine Ch1: 94 cuts, 0/0/0 — fully clean.
- Omniscient Reader Ep0(Prologue, dir Episode_1): 24 cuts, 2 WARN — ghost p000019 (0.04, eyeballed negligible), echo g0002 (narration mentions in-story novel TITLE on the premise doc panel — legit).
- Infinite Evolution Ch1: 87 cuts, 7 WARN — ghost p000085_b (0.06, negligible; "7.98s" stopwatch callout correctly kept), 6× caption-read-aloud echoes (exposition captions visible + narrated — standard recap form; IE narration regenerates in the redo anyway).
- Eyeball evidence: /tmp/qa_*.jpg contact sheets; all flagged panels reviewed by eye this session.

## ⛔ Pending USER decisions (do not render before these)
1. **Approve QA state → run renders?** Plan: Omniscient + Infinite = full redo (`/tmp/redo_chapters.sh`: groups→cast→beats→script→Modal voice→plan→prep→render, ~$0.4 beats + ~$0.74 voice, ~2.2h) — their narration still references pre-chrome groups. Nano = `/tmp/nano_rerender.sh` (prep+render ONLY — narration/voice LOCKED approved). prep_qa must re-run+pass after each prep, BEFORE each render (insert into scripts).
2. **YOLO tiled inference** (root cause: detection at 640px letterbox → ~50-100px panels): build now then full reprocess all 3 (~$2, hours, invalidates scenes→vision→groups→beats→voice) vs render-now-tile-later for future chapters. Tiling math is TDD-able in `studio/detect/yolo_panels.py` (1280-tall windows, ~128px overlap, offset boxes, IoU-NMS keep-larger).

## Key invariants (unchanged)
- Narrator voice locked (`assets/voice/narrator_ref.wav`, Qwen3-TTS Base on Modal, ~$0.37/ch). Verbatim narration ($0). Remotion renderer. NEVER acronyms with user (write "Omniscient Reader", "Infinite Evolution").
- Suite: `.eval_venv/bin/python -m pytest -q` → 367 passed, 1 skipped. Committed through 3748cea.
- Known TODO: elftoon watermark stamp on art (template inpaint, #14, low priority); episode-bundling for short prologues; dashboard v1 next after renders (roadmap in repo history).
