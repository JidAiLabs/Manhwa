# .continue-here — 2026-07-03 11:45 — PROSE-FIRST NARRATION SHIPPED + SEAM AUDIT; ch1 QA-GREEN, awaiting user read/listen

## ⏸ WHERE WE ARE
**ch1 (310) = scripted+planned, QA ZERO-ERROR** with the new prose-first narration
(21 beats, 78 segments, span mix {1:53, 2:19, 3:4, 4:2}; 27 WARN report-only).
Review at `http://10.88.0.1:8170/chapter/310`. Old video/voiceover DELETED (user
asked); approvals cleared → **voice + render wait on the user's verdict**. Rejected
narration backed up: `manifest.beats.pre-prose-BAK.json` (+ rewind BAKs).

## ✅ SHIPPED TODAY (main = `420300b`, Mini deployed, dashboard daemon restarted; worker daemon untouched)
1. **`baf237e` prose-first narration** — the 4-week fix. Free generation asks gemma for
   ONE connected passage (the user-approved grouped-era voice) + panel-tagged
   `sentences`; `segments_from_sentences()` derives spans in code (owners never
   regress, untagged panels ride the previous span, system solo, cap-4 overflow rides
   next sentence). Same `segments[]` contract; pinned heals keep direct-segments.
   NEVER go back to asking the model for spans (3 prompt fixes failed; user rejected).
2. **`a64fe52` filename gate ×3 layers** — prose-first hands gemma scene_file names as
   tags; it voiced "…to conclude at p000032.jpg." Writer meta-retry + validate_segments
   re-ask + prep_qa `filename_in_narration` ERROR (healable).
3. **`8697aab` span-pin only when voiced** — the pin protects the TTS clip cache; on
   unvoiced ch1 it guarded an EMPTY dir and rejected 9/9 good heal rewrites (user saw
   unchanged narration). Corrections pin only when `tts/tts_index.json` exists; pads
   still never replace real lines on either path.
4. **`420300b` seam-audit batch** (3 parallel audit agents over the whole chain):
   - `studio/catalog/reset.py rewind_chapter()` — THE atomic chapter rewind (files +
     stage_run history + approvals + status together). Dashboard rebuild/revoice
     buttons route through it. New CLI: `studio reset <sid> --chapters N
     --to scripted|grouped|detected|downloaded [--keep-base] [--enqueue]`.
     Kills the stale-readiness class ("prep 2 · voice 1" after deletions).
   - TTS cache keys on exaggeration too (mood-only escalation used to reuse
     old-intensity audio).
   - validate_segments rejects shot-description lines at the writer (the g0015
     3-cycle heal burn now = one repair re-ask at source).

## AUDIT VERDICTS (agents, evidence-backed)
- OK-verified: pipeline order writer→punchup→script→QA (QA sees the VOICED text);
  punchup can't re-split spans + budget-gates rewrites; script 1 shot/segment,
  span order preserved; TTS cache clean after tts/ deletion; planner multi_cut
  floors per panel; segment_id renumbering-safe.
- PARKED (non-blocking): accept_better may desync narration_plain on revert
  (only under opt-in `semantic_heal`); filler-segment orphan clips (pruned at next
  voice); readiness counters trust stage_run (atomic rewind restores the invariant);
  sauce_density ~11% vs 25% target (persona-density lever, user call); healed lines
  drift to past tense occasionally (no QA tense check — flag if user notices).

## ch1 APPROVED (2026-07-03 ~12:00) + one hand-fix
User verdict on the prose-first ch1: **"this looks perfect"** except ONE issue: 3 segments
at the g0014/g0015 boundary had narration offset one position from their panels (gemma's
sentence tags ran one panel ahead of its prose; the line_plain grounded originals confirmed
it). Fixed by a user-directed 3-line ROTATION in manifest.beats.json (text+line_plain moved,
spans untouched, joins rebuilt) — NO regen, NO re-roll. Rollback points: git tag
`ch1-approved-narration-v1` + `ongoing/nano-machine/Chapter_1/approved-narration-v1.tar.gz`
(pre-rotation manifests+tts index). If tag-shifted narration RECURS on other chapters, build
a vision-grounded line↔panel order check; observed once so far — don't build it speculatively.

## NEXT
1. **User reads/listens ch1** → approve voice → voiceover job → render (all gated).
2. ch2 (311) + ch3 (312): OLD per-panel narration — MUST
   `$V -m studio reset 2 --chapters 2-3 --to grouped --enqueue` (never raw SQL again).
3. If ch1 approved: ch4–10 same flow.

## OPS NOTES
- Mini dashboard binds 10.88.0.1:8170 (NOT localhost) — health-check that address.
- Dashboard daemon label: `com.originpower.dashboard` (worker: `com.originpower.worker`);
  restart = `launchctl kickstart -k gui/$(id -u)/<label>`.
- ch1's old stage_run rows (voiced/render_segment) were hand-deleted BEFORE
  rewind_chapter existed; future rewinds do it automatically.

## Commit trailer: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
