# CONTINUE HERE — 2026-06-30 (FIX A+B SHIPPED; clean-slate ch1-10 run; NEW blocker: system_card_unshown)

## ✅ SHIPPED THIS SESSION — commit `d9a4ec3` (pushed to origin/main; Mini deployed; 1147 tests green)
- **FIX A** `tools/prep_qa.py`: dropped the stale `missing_branding`/"expected an outro" WARN. Per-chapter QA expects NEITHER intro nor outro (outro removed in 3ea4271; intro is bundle-level). `no_cold_open` kept.
- **FIX B** `tools/panel_understand.py` `apply_system_card_overrides()`: deterministic system-card classification via the trained system_box YOLO (`assets/models/webtoon_panels.pt` class 1). Promotes gemma `caption`/`empty`→`system` ONLY when (a) not `story`, (b) not a speech-bubble (`_describes_speech_bubble`), (c) system_box coverage ≥0.20. Fail-soft. Runs in main() after the in-world rescue; stamps panel_kind onto vision.json → flows to story_group AND render_prep. **VERIFIED FIRING in prod**: ch1 understood p000114=system; log `[system-card] ...caption->system`.
- **GOTCHA**: FIX B only takes effect on a grouped re-run (delete `manifest.panels.understood.json` / reset to visioned/downloaded). Idempotent pipeline skips grouped otherwise.

## 🔴 NEW BLOCKER (top priority next session) — FIX B exposed `system_card_unshown`
FIX B promotes MORE panels to `system`. prep_qa's invariant = **a `system` panel MUST be shown in a cut** (`system_card_unshown`, BLOCKING). But the planner/render path does NOT always show a newly-promoted system panel → the chapter is BLOCKED at the QA gate (parks at `scripted`, autopilot won't advance).
- **Proven on ch2** (`ongoing/nano-machine/Chapter_2`): FIX B promoted p000011 (cover 1.00) + p000043 (0.54) to system. p000043 IS shown; **p000011 is NOT in render.plan.clean.json** → `system_card_unshown` ERROR → auto-heal can't fix (`[heal] no progress`) → prepare FAILS.
- This is FIX B + the QA gate correctly surfacing that a story-critical card is being dropped by the render path (fail-loud, not silent). **Follow-up = make grouping→planner→render GUARANTEE every panel_kind=system panel is shown** (render_prep already has system_files exempt_from_drop/protect per 8323dda — find why p000011 still isn't shown; likely story_group/planner drops/merges it before render_prep sees it). Reproduce: inspect ch2 `manifest.groups.json` + `render.plan.clean.json` for p000011's fate.

## LIVE RUN STATE (clean-slate ch1-10, Nano Machine series 2, autopilot ON)
- User cleared the DB to scratch this session (see [[clean-slate-reset-procedure]] UPDATE 2026-06-30) and activated ch1-10. Worker UP on Mini.
- **ch1 ✅ rendered, ch4 ✅ rendered** (autopilot auto-approved on spotless QA → voice → render).
- **ch2,3,5,6 🔴 FAILED prep-QA (parked at `scripted`)** — BLOCKING codes: ch2 `system_card_unshown`(+cross_dup,fragment_dangle); ch3 `empty_item,system_card_unshown,visible_text`; ch5 `system_card_unshown`; ch6 `visible_text`. (`visible_text`/`empty_item`/`cross_dup`/`fragment_dangle` are pre-existing QA codes catching imperfect chapters; `system_card_unshown` is the FIX-B-linked one above.)
- **ch7 grouped (running), ch8-10 downloaded (queued).** As ch8-10 finish they may hit the same blockers.
- **Backup before wipe** (Mini, OUTSIDE repo, reversible): `~/manhwa_scratch_backup_20260630-105235/` (studio.db.bak + manifests_and_logs.tar.gz).
- **Intro**: bundle 2 = ch1-10; auto-teaser fires only when ALL 10 reach `rendered`. With 4 parked, it WON'T fire until they're unblocked (or manually approved). teaser_state was reset to none by the wipe.

## 🔴 USER-REPORTED OUTPUT-QUALITY DEFECTS (Nano ch1 video) — FIX NEXT SESSION (+ FULL AUDIT)
Full detail + grounded data in memory [[recap-quality-issues-2026-06-30]]. Summary:
1. **Over-drama + over-tense voice on minor events** (MC just fell). GROUNDED: ch1 intensity = intense 66 / tense 34 / calm 15 / explosive 1 of 116 (**57% intense**). Recalibrate intensity + soften intensity→mood/exag mapping.
2. **ZERO dialogue surfaced** (prev runs had "Ancestor!", now none). HYPOTHESIS: commit 2390d77 "paraphrase, never quote" stripped ALL quotes (undid D5 "invite short quotes"). Verify in beats narration / tts_text.
3. **Big-group flash + bad line**: GROUNDED — `g0010_p14` = 12 panels in 2.5s (~0.21s/panel); groups up to 15 panels while others are 1-2. Split big groups / add a planner min-per-panel floor / promote flash gate to BLOCKING.
4. **No persona / no fun** (flat narration). GROUNDED LINKAGE: punchup IS wired (pipeline.py:274-289, `punchup="cinematic"`) but cinematic gives FULL persona only to CONNECTIVE (low-intensity) beats; with 57% intense, persona is suppressed everywhere. **Issues 1 & 4 share one root: over-escalated intensity starves the persona pass.**

## ⚠️ OPPOSING-FACTS ALERT (user wants a FULL AUDIT of rules/modules/persona vs current approach)
- **A.** Intensity 57%-intense ⟷ cinematic-punchup gives persona only to CONNECTIVE beats → persona starved (root of #4).
- **B.** D5 "invite short quotes" ⟷ 2390d77 "paraphrase, never quote" → 0 quotes (#2). DIRECT contradiction.
- **C.** `tools/recap_style.py` (NEW, untracked) is NOT wired into pipeline/worker/toml — a persona module that never runs.
- **D.** VERIFY `gemini_verbatim` voices the PUNCHED-UP `narration`, not plain `narration_plain` (else persona never reaches TTS).
- **No canonical "modules a,b,c,d" list in notes — ASK the user what a,b,c,d are**, then audit each persona rule/module end-to-end to TTS.

## MEMORY WRITTEN THIS SESSION (auto-loads next session)
`system-card-override-and-outro-fix.md` (the 2 fixes + the system_card_unshown gap), `source-fetch-rate-limit-posture.md` (all 3 sources rate-limit-safe; lazy fetch; gpu-lane serial gap — verified live), `recap-quality-issues-2026-06-30.md` (the 4 user-reported defects + opposing-facts alert), and updated `clean-slate-reset-procedure.md` (hard-stop launchctl bootout variant + scoped-delete safety + in-flight-child race).

## DECISIONS FOR THE USER (next session)
1. **Unblock the parked chapters**: the right fix is the `system_card_unshown` follow-up (show promoted system panels). Quick unblock for THIS batch only: manually review+approve, OR (risky) drop the show-ability invariant. Recommend FIXING show-ability.
2. The `visible_text`/`empty_item` failures (ch3,ch6) are separate render-cleaning/timeline nits to triage from each chapter's `prep_qa.json`.
3. Don't expect the auto-intro until all 10 are unblocked + rendered.

---
## (SUPERSEDED — 2026-06-29 handover kept for reference) REGRESSION: render_prep drops 76/113 distinct panels

## ✅ PANEL-COLLAPSE REGRESSION — FIXED + VERIFIED END-TO-END (637357a, deployed @ Mini)
Re-ran ch1 full pipeline (job 534 re-group+re-narrate → 535 voice → 536 render). MEASURED on the real output:
distinct panels **38 → 98**, runtime **3:51 → 8:24**, beats parse_errors **2 → 0** (real narration, not backfill),
black frames **3 → none**, shots 8(max35) → 11(max15). Delivered: `~/Desktop/nano_ch1_allpanels.mp4`.
The 4 fixes in 637357a: (1) script_expander honors per-panel lines on error beats; (2a) gemini un-flags
backfilled error beats; (2b) story_group DEFAULT_MAX_BEAT_LEN=15; (3) timeline_planner caption-fold w/o held
dup; (4) render_prep protects narrated_files from dedup. ROOT CAUSE was commit 9ee1352 (Jun 21) silencing
oversized-group narration + uncapped group size making gemma parse-fail. render_prep was INNOCENT (dropped 1/76).

## REMAINING (updated)
1. **System card** — ✅ FIXED + deployed (`d9a4ec3`). `tools/panel_understand.py apply_system_card_overrides()`:
   trained system_box YOLO (webtoon_panels.pt, class 1) fires on p114 (cover 0.89) BUT also false-fires on a
   story strip (p005) + bubble husk (p020) → GUARDED override: promote caption/empty→system ONLY when (a) gemma
   kind is caption/empty (never demote story), (b) NOT a speech bubble, (c) system_box coverage ≥0.20. Fail-soft
   if weights missing. CLI: --panel-weights/--device (defaults = render_prep's). **Only applies on a re-run that
   re-does understanding → DELETE understood.json first.** Verifying now via ch1 re-run (job 542: reset to
   visioned, understood.json deleted → expect p114 flips caption→system, kept+shown).
2. **Stale `missing_branding` WARN** — ✅ FIXED (`d9a4ec3`, prep_qa drops the no-outro warn).
GOTCHA (autopilot park): even with stale-outro fixed, grounding_weak + held_repeat WARNs remain → autopilot
needs SPOTLESS QA to auto-advance, so it PARKS at scripted. For a hands-off ch1-10 batch, either (a) loosen
autopilot to advance on non-blocking WARNs (worker.py change=restart) or (b) accept manual voice/render approve
per chapter. PRE-APPROVING render makes autopilot SKIP enqueueing render_segment (only enqueues when NOT
approved) → enqueue render_segment manually after voiceover. (historical remaining below)
3. **~6 grounding_weak WARNs** — gemma judge flags narration naming things not in-panel (glowing cylinder,
   assassin). Narration-quality nits; non-blocking.
4. **held_repeat p097×4** — caption-fold reduced but one 4-consecutive hold remains.
5. **Autopilot park**: with non-blocking WARNs present, autopilot parks at scripted (needs spotless QA). Had to
   review-approve voice+render manually. GOTCHA: pre-approving `render` makes autopilot SKIP enqueueing
   render_segment (it only enqueues when NOT approved) → had to enqueue render_segment manually. Status reached `rendered` via jobs 535(voice)+536(render).
6. **Intro** needs ch2-10 prepped (auto-intro wired 3ea4271; bundle 2 = ch1-10).
7. **Duration 8:24 vs old ~10min**: real-gemma narration is tighter than the old backfill; if longer/richer
   per-panel narration wanted, that's a narration-prompt dial (separate from panel fix).

## (historical) ROOT CAUSE detail (workflow wf_c2715ca1-f0f)
**NOT render_prep (it drops only 1 of 76). The real chain (proven on live ch1 data):**
1. **62/76 panels (ROOT):** `story_group.py:548 max_beat_len=0` (unbounded) → groups 4 (29 panels) & 5 (35 panels) too big for gemma4:26b → `parse_failed_after_retries` (error beat). gemini_narrative_pass STILL backfills VALID per-panel lines (so beats LOOKS complete = 113). Then **`script_expander.py:899` `panel_lines = [] if b.get("error") else ...` DISCARDS those valid lines** for error beats → each big group collapses to ONE "The scene continues." shot showing only allowed[:1]. **REGRESSION = commit 9ee1352 (2026-06-21).**
2. **~12 (SECONDARY):** timeline_planner `drop_caption_cards` (~1328) drops caption cards (incl. system card p000114) AND HOLDS the neighbor to fill → THAT is what makes p097 repeat 3× (held substitution). render_prep `cap_repeats_with_holds` only marks them held — symptom, not cause.
3. **1-2:** render_prep drop_near_identical (p000016 vs p000013, NCC≥0.96).
My earlier num_ctx fix (19312eb) treated a symptom — the group STILL parse-fails (too big for gemma), it just no longer errors on context.
**FIX (being implemented, TDD):** (1) script_expander:899 honor panel_narration on error beats + filter placeholder lines → restores 62. (2a) gemini_narrative_pass rename error→group_parse_error after valid backfill (stop silencing). (2b) story_group max_beat_len default ~15 (gemma stops parse-failing → REAL narration; does NOT cap per-panel narration length). (3) timeline_planner drop_caption_cards: fold caption into adjacent panel, NO held duplicate → kills p097×3 + restores ~10. (4) render_prep: pass narrated_files as protect into the 4 drop_* dedups → no narration-bearing panel dropped. KEEP: merge_consecutive_same_image (p092 Ken Burns), caption fold, system-card keep, D1 redistribute.
**VERIFY after fix:** re-run ch1 → render.plan.clean.json shows ~113 distinct panels (not 38), runtime back to ~10min, no p097×3, system card p000114 present. Earlier same-file-repeat complaint is MOOT (all 113 beats are DISTINCT files).

## STILL PENDING (after the panel-collapse fix)
- **System card p000114** misclassified `caption` by fresh gemma understanding (non-determinism) → folded, not shown. Fix: deterministic system-card override using the trained system_box YOLO detector at the understanding stage (render-side 8323dda is fine; miss is upstream).
- **Intro** needs ch1-10 (auto-intro wired in 3ea4271: bundle 2 = ch1-10, all-rendered → auto teaser → concat intro+ch1). User ran ONLY ch1 → no intro yet.
- **Pacing/length** restores naturally once all panels are shown (113 panels paced under their narration ≈ the ~10min target).

## (prior state) CLEAN SLATE + auto-intro wired; ch1 full run done

## STATE: full clean slate done, ch1 running from scratch (job 528)
- **Mini** (`jidailabs@10.88.0.1`, `~/repos/Manhwa`) on `main @ 3ea4271`. Worker+dashboard live (`10.88.0.1:8170`). Autopilot ON for series 2. Worker daemon RESTARTED (3ea4271 touches worker.py).
- **CLEAN SLATE (user wanted "new manhwa"):** ALL Nano series-2 generated files wiped (0 manifests; only source NNN.jpg kept), all series-2 job/stage/approval DB rows cleared (dashboard "recent chapters" now empty), all chapters reset to discovered/downloaded, `dist/bundle_*` + all job logs deleted, teasers reset. DB backups: `studio.db.bak-fullwipe-*`. GOTCHA hit: zsh aborts on unmatched globs — use `find ... -delete`, not `rm *.json`.
- **AUTO-INTRO WIRED (3ea4271):** bundle 2 = ch1-10 is the SOLE intro bundle (deleted stale ch1-8 bundle 1). When ALL 10 bundle chapters reach `rendered` + autopilot on → auto plan_teaser (scores hook across ch1-10) → auto-approve → concat teaser+ch1 → `dist/bundle_2/intro_ch1_FINAL.mp4`. Fires once (teaser_state + job-dedupe guard). OUTRO REMOVED (render_prep `outro_dur=0.0`; ends on last panel; watermark kept).
- **ch1 (310) running (job 528, prepare, autopilot)** from the clean slate — full re-process source→stitch→scenes→vision→understand→beat→script→QA→voice→render. Poller watching → rendered.
- **OPEN DECISION (user):** intro needs ALL ch1-10 rendered; user queued ONLY ch1 (bundle 0/10) → intro WON'T fire from this run (ch1 video only). Options given: (1) queue ch2-10 (~8-10h serial), (2) verify ch1 first then queue rest [recommended], (3) rewire intro to fire on ch1-10 *understood* (not rendered) so only ch1 renders fully.
- **VERIFY ON ch1 OUTPUT (pull frames, NOT the plan):** no outro at the end; bubbles = clean art, text removed, no husk; system cards (e.g. p000114 "7TH GEN NANO MACHINE") SHOWN with text; no black frames; no same-image repeats; narration has NO shot/effect-description ("motion blur", "a close-up shot shows"), a few short direct quotes OK.

## 5 DEFECTS FIXED THIS PASS (root causes)
- **D1 black screens** (final 1:12/9:46/11:30): `render_prep.cap_repeats_with_holds` only re-flowed time when ALL cuts in a segment dropped; dropping SOME left a no-cut time-hole → #000. FIX `adca02f`: redistribute survivors across the full window on partial drop. + `cut_gap` WARN→ERROR backstop (`b7da6b9`, prep_qa) so a gap can never ship silently.
- **D2 same image twice, same animation** (p000091, p000109): caption-only panels drop but their narration segments survive → planner tiles the same story panel NON-adjacently → merge (adjacent-only) misses it. FIX `adca02f` (Route A): group-global repeat cap — never re-emit a non-exempt panel within a group; hold the prev distinct panel instead. (Route B upstream caption-fold skipped — re-keys TTS for no visible gain.)
- **D3 empty-bubble husk on STORY art** (p000007 white circle): bubble-blank kept the outline (no inpaint). FIX `adca02f`: `clean_panel_image` leaves panel_kind==story art UNblanked (recap paraphrases); still blanks caption/system.
- **D4 "a close-up shot shows…" narration** (p34/p36/p84): `align_panel_narration` fallback pad copied the understanding DESCRIPTION (camera prose) verbatim when the model skipped a panel. FIX `b7da6b9`: `is_shot_description()` detector + pad prefers action/non-camera/subjects/bridge + prep_qa ERROR + narration_heal HEALABLE + prompt rule. NEEDS re-narration to fully take effect.
- **D5 no direct dialogue**: prompt framing suppressed all quotes; sfx_scrub dropped short dash quotes. FIX `b7da6b9`: prompt INVITES a few short (≤6w) sentence-case attributable quotes; sfx_scrub drops a dash quote only when contentless. NEEDS re-narration.

## PRIOR FIXES (already shipped + deployed this session)
- `8323dda` system-card kept unconditionally (panel_kind=system) + worker NonRetryableError (no retry-burn on deterministic QA). `212bc76` dashboard chapter_history = last-run not lifetime-sum (the 4.5h bug).

## WHAT WAS FIXED THIS SESSION (the "3 weeks of same shit")
**Root cause = empty-bubble ⇄ system-card ping-pong + deterministic retry-burn.** An empty dialogue
bubble (must DROP, text→narration) and a system/notification card (must KEEP+SHOW, text IS content)
were visually identical to render_prep's heuristics → every fix for one broke the other. Commit
9118b8b's `exempt_from_drop` gated the system-card husk-exemption on `recoverable`, so the real
"7TH GENERATION NANO MACHINE" card (p000114, panel_kind=system) got dropped → `system_card_unshown`
(BLOCKING) → worker auto-retried the identical 30-40min prepare 3× (520→521→522 ≈ 90min) → gave up.

- **8323dda** — `tools/render_prep.py`: a panel that is `sys_box OR title_card OR panel_kind=="system"`
  is UNCONDITIONALLY exempt from husk-drop AND never bubble-blanked; all 4 dedup fns gained a
  `protect` set so a system card is never removed by seam-dup. Empty bubbles stay excluded UPSTREAM
  as `caption` (panel_understand/story_group) — the two no longer fight. `studio/worker.py`: new
  `NonRetryableError`; deterministic QA-blocks (`_CRITICAL_QA_CODES`) fail ONCE and park — no
  retry-burn. Transient errors (429/timeout/exit124/IO) still retry. (1106 passed.)
- **212bc76** — `studio/dashboard/jobs.py` `chapter_history()`: "recent chapters" total was a
  LIFETIME SUM across all attempts (ch1 = 4.5h from 23 debug re-runs). Now shows the LATEST attempt
  per stage (last-run cost ≈ 16min for ch1), stops accumulating. (1107 passed.)

## VERIFIED (the user's open complaints, all green on this run)
- **System card SHOWN:** p000114 `panel_kind=system`, present in render.plan.clean.json, no `system_card_unshown`.
- **Empty bubble GONE:** p000020 `panel_kind=caption`, folded, not a shown cut.
- **Repeated image = ONE continuous animation:** p000092 shown across g0018_p34/35/36 (3 narration
  lines) as ONE file with ONE continuous Ken Burns (zoom 1.0→1.1 sliced, no reset). Per-panel
  narration plays back-to-back. This is `merge_consecutive_same_image_cuts`' cross-item continuous slice.

## HOW TO RE-RUN A CHAPTER (fast path, via worker)
If understanding+beats+script already correct (only render/QA changed), reset to `scripted` NOT
`visioned` to skip ~50min Gemma: `update chapter set status='scripted'`; `delete from approval where
chapter_id=?`; rm render.plan*.json prep_qa.json heal_corrections.json; then
`from studio.dashboard.jobs import enqueue; enqueue(con,"prepare",chapter_id=310,priority=1)`.
Autopilot (series.autopilot=1) chains prepare→voiceover→render hands-off.

## OPEN BUG (not fixed — real, has manual workaround)
**Re-preparing a chapter doesn't clear its old `approval` rows** → autopilot's `not _has_approval("voice")`
skips auto-advance → parks at `scripted`. Workaround: `delete from approval where chapter_id=?` before
re-run. FIX: clear approvals on reset/re-prepare, OR autopilot ignores stale approvals on spotless QA.

## GOTCHAS
- ALWAYS run via worker/dashboard (enqueue jobs) — NOT manual nohup. (user firm)
- worker.py/dashboard change → `launchctl kickstart -k gui/$(id -u)/com.originpower.{worker,dashboard}`.
  tools + pipeline.py are subprocesses → fresh on `git pull`.
- ffmpeg over SSH: launch worker with `PATH=/opt/homebrew/bin:$PATH` (non-interactive SSH drops it).
- `.mlx_venv` untracked-NOT-ignored → `git stash -u` breaks TTS; recover `git checkout 'stash@{0}^3' -- .mlx_venv`.
- Never kill narrate/heal mid-write → corrupts beats to a stub → truncated render.
- Dashboard pushes went to `main` directly (repo's normal flow); flag if a branch/PR is wanted.
